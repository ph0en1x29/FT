# FT Performance Digest — 2026-05-16

Generated by scheduled remote agent. Read-only telemetry only.

## TL;DR

All database queries in this digest failed to execute because the remote execution environment's network policy blocks outbound connections to the Supabase pooler (port 6543, ETIMEDOUT) and the direct host DNS does not resolve from this container (ENOTFOUND). No telemetry data was collected. The digest structure is preserved so it can be populated on the next run from a network-capable environment (e.g., running the agent locally or from an environment with an allow-listed egress policy for `*.pooler.supabase.com:6543` and `db.*.supabase.co:5432`). Round 5 (trigger no-op guards / van_stock_usage unique constraint) cannot be evaluated — defer to the next successful digest cycle.

## 🟢 Wins

No data available this cycle. Network connectivity to Supabase was blocked by the remote container's egress policy. Prior state (2026-05-01 baseline) for reference:

- 0 active duplicate `job_assignments` rows
- 1 historical duplicate `van_stock_usage` row (2026-04-27, job `3835cd0e-bd6f-485b-a8bc-1a4598d2fd7f`)
- 20 jobs pending AutoCount export
- 834 active jobs, 3306 parts catalog rows

## 🟡 Watch

- **Network egress policy**: This remote agent cannot reach `aws-0-us-west-2.pooler.supabase.com:6543` or resolve `db.dljiubrbatmrskrzaazt.supabase.co`. For future scheduled digest runs to succeed, the execution environment must allow outbound TCP to those endpoints. See "Recommended next actions" for remediation options.
- **Round 5 gate**: The decision to apply trigger no-op guards and the `van_stock_usage` idempotency unique constraint depends on confirming zero new duplicate `van_stock_usage` rows since baseline. This remains unverified for ~2 weeks post-overhaul.

## 🔴 Act

- **Fix network policy before next digest cycle.** Without egress access to Supabase, automated telemetry is impossible from this environment. Options are listed under "Recommended next actions."

## Detailed metrics

> **Note:** All queries below failed with network errors. Results are shown as errors rather than data tables. Error details are included verbatim.

### Top 20 slowest queries (pg_stat_statements)

**Error:** `connect ETIMEDOUT 54.70.143.232:6543, 44.238.118.41:6543, 35.160.209.8:6543` — outbound TCP to Supabase pooler port 6543 blocked by container network policy.

Query that would have been run:
```sql
SELECT round(total_exec_time::numeric, 2) AS total_ms,
       round(mean_exec_time::numeric, 2) AS mean_ms,
       calls,
       LEFT(query, 200) AS query_preview
FROM pg_stat_statements
WHERE query NOT ILIKE '%pg_stat_statements%'
  AND query NOT ILIKE '%pg_catalog%'
ORDER BY total_exec_time DESC
LIMIT 20;
```

### Trigram + low-stock index usage

**Error:** Same network failure — connection could not be established.

Indexes that would have been checked: `idx_customers_name_trgm`, `idx_customers_phone_trgm`, `idx_customers_account_trgm`, `idx_customers_email_trgm`, `idx_customers_address_trgm`, `idx_parts_name_trgm`, `idx_parts_code_trgm`, `idx_parts_low_stock`, `idx_parts_out_of_stock`.

### Unused indexes >1MB

**Error:** Same network failure — connection could not be established.

### New RPC call counts

**Error:** Same network failure — connection could not be established.

RPCs that would have been checked: `get_job_status_counts`, `get_inventory_catalog_stats`, `get_low_stock_part_ids`, `get_jobs_pending_export`, `fulfill_replenishment`, `confirm_replenishment_receipt`, `skip_notifications_for_deleted_jobs`.

Note: Even if the connection had succeeded, `pg_stat_user_functions` only tracks calls when `track_functions = pl|all`. If that GUC is not set, all call counts would be 0 regardless of actual usage — this should be verified on the first successful run.

### Duplicate-row audit

**Error:** Same network failure — connection could not be established.

- Active duplicate `job_assignments`: **unknown** (baseline: 0)
- Duplicate `van_stock_usage` rows: **unknown** (baseline: 1, from 2026-04-27)

### Table sizes

**Error:** Same network failure — connection could not be established.

Tables that would have been checked: `jobs`, `parts`, `customers`, `job_parts`, `job_media`, `notifications`, `van_stock_items`, `van_stock_usage`, `hourmeter_amendments`, `autocount_exports`.

## Recommended next actions

1. **Fix the egress policy (required for any future digest run).** The remote container must allow outbound TCP to:
   - `*.pooler.supabase.com:6543` (PgBouncer pooler — used by default)
   - `db.dljiubrbatmrskrzaazt.supabase.co:5432` (direct Postgres — fallback)
   When creating the next Claude Code on the Web session for a digest run, select a network policy that permits database egress, or run the agent script locally from a machine with unrestricted outbound access.

2. **Re-run this digest from a network-capable environment.** The `telemetry_run.mjs` script committed alongside this digest (or cleaned up if the author prefers) contains all six query sections and outputs JSON. Running `node telemetry_run.mjs` from the repo root on a local machine will produce the raw data needed to populate this template.

3. **No Round 5 action recommended at this time.** Round 5 (trigger no-op guards on hot tables + `van_stock_usage` idempotency unique constraint) was explicitly deferred pending confirmation that no new duplicate `van_stock_usage` rows have accumulated since the 2026-05-01 baseline. That data was not obtainable this cycle. Do not apply Round 5 until a successful digest confirms the duplicate count has not grown above the 1 historical row.

4. **Verify `track_functions` GUC on next successful run.** If the RPC call-count section shows all zeros, check `SHOW track_functions;` and set it to `pl` or `all` if needed for meaningful telemetry.

## Sentry web-vitals

Sentry web-vitals telemetry was not accessible from this agent. No Sentry MCP connector is configured for this run. To enable Sentry data in the next digest cycle, connect a Sentry MCP at https://claude.ai/customize/connectors, then add a "Sentry" section to the digest template querying the relevant project's error rates, p75/p95 LCP and CLS for the FT frontend. This would complement the DB-side metrics with real-user performance data.
