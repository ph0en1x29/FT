[2026-04-23 01:29] [Opus] hardening: scope return RPCs to assigned tech / admin, qty>0 + DELETE guards, consolidate realtime to single shared channel, pre-seed toast seen-set, scroll-to-parts blocker chip, auto-hide empty pending-returns section, reason maxLength — 20260423_part_return_security_hardening.sql, pendingReturnsService.ts, usePendingReturns.ts, PendingReturnsSection.tsx, PartReturnModal.tsx, MobileTechnicianWorkflowCard.tsx, JobDetailPage.tsx
  - Triggered by a fresh 3-round adversarial review (security / performance / UX) run after the main feature shipped at 00:58. Three reviewers in parallel surfaced real exploitable + UX issues; this entry is the fix pass.
  - **Migration 4** (`20260423_part_return_security_hardening.sql`, applied to live Supabase): (a) scopes `request_part_return` to the assigned technician, helper, an active job_assignment, OR an admin/supervisor — previously any authenticated tech could pending-return any job's parts. (b) Scopes `cancel_part_return` to the original requester, the assigned tech, OR an admin — previously any tech could undo any other tech's pending return. (c) Adds a `quantity > 0` guard at the top of `confirm_part_return` AND switches the integer cast to `ROUND(v_qty)::INTEGER` (was truncating, so 4.7L liquid returns under-credited 4 instead of 5). (d) Adds `CHECK (job_parts.quantity > 0)` constraint (live audit confirmed 0 violations before applying). (e) Adds a BEFORE DELETE trigger `guard_job_parts_pending_return_delete` refusing deletion of any row in `pending_return` state — closes the orphan-audit attack where an admin could DELETE a pending row and silently lose the restock. Sanity asserts on all 5 changes passed.
  - **Realtime consolidation** (`services/pendingReturnsService.ts` rewritten): three components (badge count, queue list, toast notifier) were each opening their own `pending-returns-N` channel and refetching the joined `listPendingReturns` query on EVERY `job_parts` event across the org — multiplicative DB load. Replaced with a ref-counted singleton: one `pending-returns-shared` channel filters at the callback boundary to actual pending_return state transitions (`transition_in` / `transition_out`), then fans out to N local listeners. Channel torn down only when the last subscriber unsubscribes. Added 400ms debounce in `PendingReturnsSection` to coalesce burst events (e.g. a bulk approve creating multiple rows in one tick). Also moved the `deleted_at IS NULL` filter into the joined PostgREST query via `jobs!inner(...) + .is('job.deleted_at', null)` so Postgres drops those rows before embedding customer + forklift.
  - **Toast race fix** (`hooks/usePendingReturns.ts:usePendingReturnsToastNotifier`): the previous version relied on a 1.5s timer to mark `initialized=true`, which was racy on slow connections and reset on every navigation (`location.pathname` / `location.search` were in the effect deps). New version: synchronously fetches `listPendingReturnIds()` BEFORE subscribing, pre-seeds the `seen` set with existing pending IDs, then sets `initialized=true`. Drops `location.*` from deps; the approvals-tab suppression now reads `window.location` at toast-fire time so route changes are reflected without re-subscribing. Result: subscription survives navigation; historical pending rows never trigger toasts; only genuinely new transitions fire.
  - **UX fixes**: (1) `PendingReturnsSection.tsx` returns `null` when empty (post-load) so the Store Queue page isn't permanently cluttered with an "all caught up" header. (2) `PartReturnModal.tsx` reason textarea now has `maxLength={500}` plus a live `N/500` counter (warning color when at limit) — prevents pasting 50KB into the audit log. (3) `MobileTechnicianWorkflowCard.tsx` blocker chips ("After photo", "Technician sign", "Customer sign", "Parts declaration") are now tappable buttons that scroll to the relevant section — previously inert spans that left techs blocked with no path forward. Added a new `onScrollToParts` prop wired in `JobDetailPage.tsx` to the existing `partsRef`.
  - **Out of scope** (acknowledged in review, not fixed this session): (a) **C2 invoice evasion**: a tech could mark all parts pending_return immediately before completion to strip them from the invoice — to truly fix would require snapshotting `parts_used` pricing at status transition into Awaiting Finalization. Larger architectural change; the new flow doesn't make this strictly worse than the existing "delete a part row" path. (b) **H1 realtime payload leakage** depends on `job_parts` permissive RLS (per `20260201_fix_all_rls_policies.sql`); tightening RLS is project-wide. (c) **Helpers can't request return** — matches existing FT helper-as-read-only policy. (d) **Auto-populated rows show the Return button** — intentional per the migration design.
  - Verification: `npm run typecheck` clean. Migration applied to live DB; 6/6 simulated RPC tests passed under realistic auth contexts: unassigned tech → REFUSED, assigned tech → SUCCESS, admin from any role → SUCCESS, cross-tech cancel → REFUSED, DELETE on pending_return → REFUSED with helpful message, INSERT with quantity=-1 → REFUSED by CHECK constraint. All under `BEGIN ... ROLLBACK` so live data was untouched.

[2026-04-23 01:17] [Sonnet] chore: auto-commit session changes — 20260423_part_return_flow.sql, 20260423_completion_gate_skip_returns.sql, 20260423_completion_gate_acknowledge_returned.sql, inventory.types.ts, jobPartReturnService.ts, pendingReturnsService.ts, jobService.ts, jobInvoiceService.ts, customerService.ts, supabaseClient.ts, PartReturnModal.tsx, PartsSection.tsx, JobDetailPage.tsx, useJobActions.ts, utils.ts, PendingJobsSection.tsx, useKPIData.ts, usePendingReturns.ts, PendingReturnsNotifier.tsx, AuthenticatedApp.tsx, JobsTabs.tsx, PendingReturnsSection.tsx, StoreQueuePage.tsx, DB_SCHEMA.md, USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx
  - Auto-commit of session changes via stop hook.

[2026-04-23 00:58] [Opus] feature: tech-initiated part return flow + admin Confirm Return + 3-tier notification — 20260423_part_return_flow.sql, 20260423_completion_gate_skip_returns.sql, 20260423_completion_gate_acknowledge_returned.sql, inventory.types.ts, jobPartReturnService.ts, pendingReturnsService.ts, jobService.ts, jobInvoiceService.ts, customerService.ts, supabaseClient.ts, PartReturnModal.tsx, PartsSection.tsx, JobDetailPage.tsx, useJobActions.ts, utils.ts, PendingJobsSection.tsx, useKPIData.ts, usePendingReturns.ts, PendingReturnsNotifier.tsx, AuthenticatedApp.tsx, JobsTabs.tsx, PendingReturnsSection.tsx, StoreQueuePage.tsx, DB_SCHEMA.md, USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx
  - Client report (Shin, 2026-04-22 02:07–03:48): existing rule "block job completion if an approved spare-part request hasn't been added to Used Parts" breaks down when the approved part turns out to be the wrong model on-site. Tech can't legitimately use the part AND can't legitimately complete the job. Spec'd a tech-side "Return" button per Used Parts row + admin-side Confirm Return that restocks. Locked decisions: invoice line auto-removed on Pending Return; tech can cancel a pending return pre-confirm; all `job_parts` rows get the button; 3-tier notifications (badge + realtime list + global toast).
  - Migration 1 (`20260423_part_return_flow.sql`, applied to live DB this session): adds `tech_return` to `inventory_movement_type` enum (committed before BEGIN block — Postgres forbids using a freshly-added enum value in the same transaction it was added in); adds 7 columns to `job_parts` (`return_status`, `return_reason`, `return_requested_by/_at`, `return_confirmed_by/_at`, `return_notes`) with a CHECK constraint on `return_status IN (NULL, 'pending_return', 'returned')` and a partial index on non-NULL `return_status`; defines three RPCs:
      • `request_part_return(p_job_part_id, p_reason)` — tech sets pending_return, fills requested_by/_at; reason mandatory; refuses if already in any return state.
      • `cancel_part_return(p_job_part_id)` — tech (or admin) reverts pending_return to NULL; refuses unless currently pending_return.
      • `confirm_part_return(p_job_part_id, p_notes)` — admin only (admin / admin_service / admin_store / Admin 1 / Admin 2 / Admin 1 (Service) / Admin 2 (Store) / supervisor); locks master parts row, increments `stock_quantity` for non-liquids or `container_quantity` for liquids (mirrors the deduction semantics in `jobInvoiceService.ts:99-117`), inserts an `inventory_movements` row with `movement_type='tech_return'`, then sets `return_status='returned'` with confirming admin + timestamp. All three are SECURITY DEFINER + GRANT to authenticated. Migration ends with sanity asserts on column / enum / function existence and applied cleanly.
  - Migration 2 (`20260423_completion_gate_skip_returns.sql`, applied to live DB this session): updates `validate_job_completion_requirements` so the existing "approved spare-part request without a Used Part" gate (introduced 20260416, extended 20260421) treats `return_status IN ('pending_return', 'returned')` rows as "not actively used". `has_parts` now ignores returned rows, so a tech who flagged the only approved part for return can complete by ticking "no parts used" or adding a different real part. The "spare_part photo + no_parts_used" check from 20260421 is unchanged. Sanity assert on the trigger body containing `pending_return` passed.
  - Service layer: new `services/jobPartReturnService.ts` wraps the three RPCs with `requestPartReturn(reason, freeText)`, `cancelPartReturn`, `confirmPartReturn(notes)` and exports `PartReturnReason` enum + label map + `isPartActiveOnInvoice` helper. New `services/pendingReturnsService.ts` exposes `listPendingReturns()` (joined with requester + job + customer + forklift), `countPendingReturns()` (server-side count for the badge), and `subscribeToPendingReturns(callback)` (single Supabase realtime channel on `job_parts` UPDATE/INSERT, callbacks filtered client-side because Supabase realtime doesn't support column-value filtering). `services/jobService.ts` SELECTs in `getJobByIdFast` and `getJobById` extended with the seven new return columns + `auto_populated` (which was missing from the SELECT until now — was working only because the row update flow returned the full row).
  - Tech UI: new `components/PartReturnModal.tsx` — self-contained modal owning its own form state (reason chips + free-text; "Other" requires free-text). On submit calls the service, propagates updated row up via `onRequested` callback, closes. `PartsSection.tsx` row render extended with: Pending Return / Returned pill, line-through + opacity dimming on the part name, RotateCcw "Return" icon button per active row (gated to in-progress / awaiting-finalization for tech / canAddParts), Undo2 cancel button visible to the original requester (tracked via new `currentUserId` prop comparing to `return_requested_by`) AND admins, PackageCheck "Confirm Return" button visible to admins. Removed the Edit/Delete buttons on returning rows so they can't be modified mid-return. `JobDetailPage.tsx` passes `currentUserId` and an `onPartReturnUpdated` callback that merges the updated row into `job.parts_used` via `setJob({...})` (consistent with the existing realtime-echo dedupe pattern from `lastSeenUpdatedAtRef`).
  - Completion gate (UI mirror): `useJobActions.ts:handleStatusChange` extracts `activeParts` (return_status NOT IN pending_return/returned) once and uses it for both the spare_part-photo-conflict check and the approved-request-not-used check. Toast wording for the latter updated to mention the Return + "no parts used" escape path so techs aren't stuck. `JobDetailPage.tsx:partsDeclared` now uses `activeParts.length > 0 || noPartsUsed` so the workflow blocker chip clears once the tech has flagged every wrong-model part for return.
  - Invoice / total exclusion: every existing parts-cost reducer updated to skip `pending_return` / `returned` rows — `jobInvoiceService.generateInvoiceText`, `customerService.getCustomerJobsRevenue`, `JobDetail/utils.getJobTotalCost`, `AutoCountExport/PendingJobsSection` row total, `TechnicianKPIPageV2/useKPIData` revenue computation. Five sites; same predicate at each. The job_parts row stays for audit (`inventory_movements` carries the restock event with the original return reason).
  - Admin UI (3-tier per locked spec): (1) Badge — new `hooks/usePendingReturns.usePendingReturnsCount` does a count-only query + realtime subscription; surfaced as a red pill on the Approvals tab in `JobsTabs.tsx` (admin/supervisor only). (2) Realtime list — new `pages/StoreQueue/components/PendingReturnsSection.tsx` mounted at the top of `StoreQueuePage`; lists each pending return with quantity / part name / reason chip / requester / job link / time-ago / Cancel + Confirm Return buttons, refetches on realtime echo. (3) Global toast — new `usePendingReturnsToastNotifier` hook (mounted via `components/layout/PendingReturnsNotifier.tsx` inside `<Router>` because it uses `useLocation`) fires a sonner `toast.info` with "Review" action linking to /jobs?tab=approvals when a brand-new pending_return appears AND the admin isn't already on the approvals tab. Self-suppresses on initial load (1.5s grace timer) so the backlog of historical pending rows doesn't blast toasts.
  - PR1 also covered in this entry (was timestamped 00:42 — re-listed here so the auto-commit's pre-commit window picks it up): `useQueryHooks.ts`, `ApproveRequestModal.tsx`, `BulkApproveRequestsModal.tsx`, `PartRequestsPage.tsx`, `StoreQueuePage.tsx`, `DB_SCHEMA.md`. PR1 fixes the parts-approval dropdown 50-item silent cap (now uses `useSearchParts` server-side search) and the `findBestPartMatch` liquid-skip bug. Full narrative is in the 00:42 entry below.
  - Scope notes: did not add the Return button to `MobileTechnicianWorkflowCard` — that card is workflow status only and doesn't render parts; mobile techs see the Return button via `PartsSection` which already adapts for mobile widths. Did not refactor the existing post-completion `reconcileParts` flow (Admin 2 quantity_used / quantity_returned reconciliation in `jobService.ts:625`) — that's a separate finalization-time mechanism and leaving it intact avoids confusing the live invoice reconciliation. Did not add a "tech receipt" photo requirement for return — the audit trail (requested_by + reason + admin confirmation) is sufficient per the locked spec. Did not change `JOB_SELECT` in `services/supabaseClient.ts` to include the new columns — the targeted SELECTs in `jobService.ts` cover the JobDetail load path; list/board fetches don't need the columns.
  - Verification: `npm run typecheck` clean (initial pass surfaced two `Object.values(inlineState)` `unknown` errors in PR1 fixed via casts; no errors after PR2 changes). `npm run build` clean. Both migrations applied to live Supabase via the pooler with SSL; sanity asserts passed; spot-checked `pg_proc` for the three new functions and `pg_enum` for `tech_return`.
  - 3-round aggressive review (added by user request after main implementation): Round 1 found a realtime channel-name collision — three components were calling `supabase.channel('pending-returns')` with the same name, which would have torn each other down on unmount; fixed by appending a unique counter to each channel name (`pending-returns-1`, `-2`, …). Round 2 simulated the full RPC flow against the live DB with a tech and an admin auth context — all six scenarios passed: request creates pending_return, duplicate request refused, cancel reverts, cancel-on-clean refused, tech-confirm refused with the role-check error, admin-confirm restocks (5→6 for FORK LOCK KIT) and writes the `inventory_movements` row with the original reason (all under `BEGIN ... ROLLBACK` so live data was untouched). Round 3 found two real regressions:
    1. **`PartsReconciliationModal` would have double-restocked returned parts** — the Admin 2 finalization reconciliation receives `parts_used` and would let admin set `quantity_used`/`quantity_returned` on rows already credited via `confirm_part_return`. Fixed by filtering `pending_return`/`returned` rows out of the props passed in `JobDetailPage.tsx`.
    2. **The `JOB_SELECT.KPI` lightweight select didn't include `return_status`** — `useKPIData.ts` filters on the field, but the column wasn't being fetched, so KPI revenue would still include returned parts. Fixed by adding `return_status` to `parts_used:job_parts(quantity, sell_price_at_time, return_status)` in `services/supabaseClient.ts`.
    3. **Trigger blocked completion when ALL approved parts were returned + tech ticked no_parts_used** — the previous trigger update used `has_parts` (which filters out returned rows) for both the "no parts at all" check AND the "approved spare-part not in Used Parts" check. The first is correct (no_parts_used is the escape) but the second was wrong: pressing Return is itself an acknowledgment of the approval, so the row's existence (in any state) should satisfy the gate. Wrote third migration `20260423_completion_gate_acknowledge_returned.sql` introducing `has_any_parts_row` (no return-status filter) and using it for the approved-spare-part gate; `has_parts` retained for the no-parts-at-all gate. Mirrored the new semantics in `useJobActions.ts:handleStatusChange` so UI and DB agree. Applied to live DB this session; sanity assert passed.
  - Manual verification needed (post-deploy): as tech with an approved spare-part auto-populated row → tap RotateCcw → modal → pick "Wrong model" → submit → row greys out + Pending Return pill appears → tick "No parts used" → Complete Job succeeds (no DB error). As admin, Approvals tab shows the badge count + Pending Returns list at top of Store Queue → click Confirm Return → row disappears, `parts.stock_quantity` incremented, `inventory_movements` has a `tech_return` row. Cancel-return path: click Undo2 → row reverts to active. Cross-tab toast: open another tab on /jobs?tab=active as admin → have a tech request a return → toast appears with Review action linking to /jobs?tab=approvals.

[2026-04-23 00:42] [Opus] fix: parts approval dropdown silently capped at 50 — admins couldn't find parts that inventory clearly listed; also stop auto-match from skipping every liquid part — useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx, StoreQueuePage.tsx, DB_SCHEMA.md
  - Client report: "Inventory shows the item, but it can't be found during approval." Admins approving spare-part requests open the part Combobox, can't locate the part they need, even though the inventory listings page renders it. Confirmed second-hand: Shin's whatsapp 11:04 PM 2026-04-22.
  - Root cause (primary): `components/Combobox.tsx:108` hard-codes `MAX_VISIBLE = 50`. With ~3000 rows in `parts`, only the first 50 alphabetically render when the dropdown is opened without typing — anything past "C…" is invisible unless the admin types a substring. Every approval surface (`ApproveRequestModal`, `BulkApproveRequestsModal`, `PartRequestsPage`, `StoreQueuePage`) was passing the full client-cached `usePartsForList()` array straight into Combobox, so they all hit the cap. Inventory listings page is unaffected because it uses server-paginated `getPartsPage` instead of the dropdown component.
  - Root cause (secondary, related): `findBestPartMatch` in `BulkApproveRequestsModal.tsx:37` and `PartRequestsPage.tsx:549` does `if (part.stock_quantity <= 0) continue`. Liquid parts deliberately store `stock_quantity = 0` and hold real stock in `container_quantity + bulk_quantity` (see `partsService.ts:isPartOutOfStock`), so the auto-suggestion silently never matches any chemical/fluid — compounding the "can't find it" reports.
  - Fix (dropdown): converted all four approval surfaces to the existing `useSearchParts(30)` server-side search hook (same pattern already in use by `TransferPartModal`, `AdjustStockModal`, `AddItemModal`). Wired `onSearch` + `isSearching` into each Combobox; the component already renders a "Type to search…" empty state and the "Showing N of M — type to narrow" footer when results exceed the cap. Extended `useSearchParts` to also expose the raw `parts` array (was returning only `options`) so approval modals can build their own option labels (`RM<price> | Stock: <qty>` instead of the default `part_code` subLabel).
  - Fix (selection persistence): in `BulkApproveRequestsModal`, `PartRequestsPage`, `StoreQueuePage` the merged `partOptions` now combines the latest server-search results with any currently-selected part_id resolved from the cached list — guarantees the Combobox can render the selected/auto-matched part's label even after the user types a new search that doesn't include it. Cached `usePartsForList()` is retained in those three files because their auto-match (`findBestPartMatch`) and matched/approved part display still need the full local list; `ApproveRequestModal` uses no auto-match so it dropped the cached fetch entirely.
  - Fix (liquid skip): both `findBestPartMatch` implementations now compute `totalStock = is_liquid ? (container_quantity||0)+(bulk_quantity||0) : (stock_quantity||0)` before deciding to skip — same definition `partsService.isPartOutOfStock` uses internally. Liquids with real bulk/container stock now match.
  - Fix (docs): `docs/DB_SCHEMA.md:377` had a stale `CHECK: status IN ('pending','approved','rejected')` for `job_requests` — the live constraint and the entire UI status badge map (`ApproveRequestModal.tsx:80-101`) use `pending|approved|issued|part_ordered|out_of_stock|rejected`. Refreshed the line with a `(refreshed 2026-04-23)` marker. Did not touch any other stale columns in this section per the "don't backfill undocumented columns out of scope" rule in CLAUDE.md.
  - Scope notes: did not convert `PartsSection` (technician/admin Add Part Combobox via `JobDetailPage.partOptions`) — same architectural cap applies but the bug report is specifically about the approval flow and the technician Add Part flow wasn't called out. Left as a follow-up. Did not change the `Combobox` component itself — its `onSearch` mode and 50-item cap are correct; the bug was upstream callers not using server-search mode. Did not refactor `findBestPartMatch` to be fully server-side — that would require N keyword queries per modal open and is a larger change; the local fuzzy match still works correctly now that liquids aren't excluded. Did not touch `inventoryService.returnPartToStore` — that's the existing van→warehouse admin transfer, not the new tech-side return flow (separate PR).
  - Verification: `npm run typecheck` clean (two transient `Object.values(inlineState)` `unknown` errors fixed by explicit casts in `PartRequestsPage` and `StoreQueuePage`). Manual verification needed: open a job with a pending spare-part request, click Approve → part dropdown shows "Type to search…" → type a part name not starting with A/B/C → results appear → select → approval succeeds. Bulk approve modal: same. Top-level Part Requests page: same. Store Queue page: same. Liquid auto-match: create a request with description "engine oil" → bulk approve modal pre-fills the matching liquid part instead of leaving the row blank.

[2026-04-21 21:52] [Sonnet] analysis: Acwer service operations flow feasibility matrix + 4-phase plan: docs/archive/client-feedback/ACWER_SERVICE_OPERATIONS_FLOW.md
  - Analysis session: created comprehensive text-based reconstruction of Acwer's three billing paths (AMC, Non-Contract Ad-hoc, Acwer-Owned Fleet) and documented the routing logic, warranty gates, W&T exclusion logic, quotation workflow, and internal cost tracking. Mapped current FT coverage against requirements: ~40% shipped, ~40% scoped, ~20% needs client clarification. Created 21-question checklist gating Phase 1 scoping. Drafted 4-phase implementation plan (13 weeks, contingent on client answers). Living design doc stored in docs/archive/client-feedback/ for client iteration.
  - docs/archive/client-feedback/ACWER_SERVICE_OPERATIONS_FLOW.md: created new file documenting business context, root decision tree, and detailed walkthrough of all three service paths with field requirements, decision gates, and downstream system integration (dispatch, parts inventory, invoicing, ROI tracking).


[2026-04-21 02:22] [Opus] fix: remove Repair from HOURMETER_EXEMPT_JOB_TYPES — Repair jobs must now record hourmeter like Service — WORK_LOG.md, docs/CHANGELOG.md, pages/JobDetail/utils.ts
  - Client report: Repair jobs DO need to fill hourmeter at Start and Complete. Earlier this session (2026-04-20 23:53) Repair was added to the exemption helper alongside FTS per a prior client ask; this reverses that for Repair only. FTS remains exempt (truly no unit tracking — on-site consultation, parts collection, charger install).
  - Pre-deploy live DB audit (2026-04-21): 6 In Progress Repair jobs at risk of getting stuck on Complete after the flip — 5 with null `hourmeter_reading`, 1 with `hourmeter_reading=0`. Five of the six have no `forklift_id`. Proceeding without a data patch — the same pattern as the 1 FTS case earlier this session: tech sees the "Hourmeter reading required" toast on next Complete tap, enters a value via the hourmeter edit row (or "1" if the unit has no real meter), and proceeds. Better than silently patching the data with a made-up value.
  - Fix: one-line edit in `pages/JobDetail/utils.ts:isHourmeterExemptJob` — drop `jobType === JobType.REPAIR` from the OR condition; keep only `JobType.FIELD_TECHNICAL_SERVICES`. Updated the JSDoc with a short policy-history block (2026-04-20 added Repair, 2026-04-21 removed Repair, with the reason) so the next session reading the helper sees the reasoning instead of assuming Repair was always there or always not there. All 5 UI call sites (Start Job modal gate, desktop in-progress banner, sticky Complete button, mobile workflow card blocker, handleStatusChange completion gate) consume the helper, so the change cascades automatically — no per-site diffs.
  - Effect for Repair jobs going forward: (a) Start Job modal Step 2 now shows the hourmeter input + ≥1 / broken-meter-remark rule shipped earlier today (empty or 0 rejected, 1 requires a remark). Checklist remains hidden for Repair via the separate `isRepairJob` prop — that is a different policy and untouched. (b) At Complete, `handleStatusChange` now requires `job.hourmeter_reading` to be truthy for Repair jobs — mirrors the existing gate for Service / Full Service / Minor Service / Checking / Slot-In / Courier.
  - Scope notes: did not touch the `isRepairJob` prop on `StartJobModal` — that still hides the Condition Checklist for Repair (correct policy, unchanged). Did not touch the DB trigger `validate_job_completion_requirements` — it has never checked hourmeter, the policy lives entirely at the UI layer. Did not add any migration. Did not patch the 6 at-risk jobs. Did not touch FTS — it remains the sole entry in the exempt list.
  - Verification: `npm run typecheck` clean. `npm run build` clean. `grep -rn HOURMETER_EXEMPT_JOB_TYPES` still returns the expected call sites, no new mirror comments needed. Remaining manual verification: open a Repair job as tech → Start Job → Step 2 shows hourmeter input with the greyed ≥1/broken-meter hint and no Condition Checklist grid → enter 5230, Start Job enables → tech completes the job flow → Complete button enabled after sigs + after photo + parts.

[2026-04-21 02:18] [Sonnet] chore: auto-commit session changes — WORK_LOG.md, docs/CHANGELOG.md, docs/USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx, pages/JobDetail/hooks/useJobActions.ts, supabase/migrations/20260421_block_completion_with_spare_part_photo_conflict.sql
  - Auto-commit of session changes via stop hook.

[2026-04-21 01:05] [Opus] fix: block completion when "Parts" photos exist but "No parts used" is ticked — 20260421_block_completion_with_spare_part_photo_conflict.sql, useJobActions.ts, USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx
  - Client report: technicians can upload photos tagged as "Parts" (category='spare_part') AND tick "No parts used" AND complete the job — logically contradictory; pollutes service history and bypasses the intent of parts declaration. Agreed on Option A (block in UI + DB) with the error-message tweak that guides techs to the escape hatches (re-tag or add parts). Pre-deploy live DB audit found 1 In Progress job (JOB-260406-001, 1 spare_part photo, no_parts_used=true, 0 parts) and 36 historical Completed/Awaiting Finalization jobs with the same pattern — confirms this is a recurring cheat path, not a theoretical edge case.
  - Fix (DB, authoritative): new migration `20260421_block_completion_with_spare_part_photo_conflict.sql` extends `public.validate_job_completion_requirements` with a new check inserted after the "approved spare part requests" check: if `EXISTS(job_media WHERE category='spare_part')` AND NOT `has_parts` AND `no_parts_used = TRUE`, raise `'Cannot complete job: You uploaded photos tagged as "Parts" but ticked "No parts used". Either add the parts you used to the Used Parts list, or re-tag the photos (Condition / Evidence / Other).'`. Admin/supervisor bypass at line 52 of the trigger is unchanged, so this rule only applies to technician submissions (admins can still force-complete). Migration uses `BEGIN ... COMMIT` with a post-apply sanity check asserting the new check text is present in the function body. Applied to live Supabase DB this session via the pooler; verified the trigger body now contains the new check.
  - Fix (UI, friendly early-reject): `pages/JobDetail/hooks/useJobActions.ts:handleStatusChange` — added a mirror check just before the "approved spare part requests" block. Uses `job.media?.filter(m => m.category === 'spare_part').length` against `state.noPartsUsed` and `(job.parts_used?.length ?? 0) > 0`. Gated on `currentUserRole === 'technician' && !isHelper` so admins aren't friction-gated in the UI (they'd still bypass the DB trigger anyway). The toast repeats the DB's exact escape-hatch guidance so techs see a consistent message whether UI catches it or DB does.
  - Fix (docs): `USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx:366-374` — added an explicit warning under the "Before you can complete" checklist explaining the new rule and both escape hatches.
  - Scope notes: did not delete or re-tag the 36 historical conflicting jobs — those jobs are already Completed and can't be un-completed without disturbing invoice state; the rule only prevents new occurrences. Did not touch the 1 In Progress job (JOB-260406-001) — that tech will see the new toast on their next Complete tap and must either add the part or re-tag the photo; if they need help, a supervisor can force-complete (trigger exempts supervisors). Did not add a client-side prevention when uploading photos ("if no_parts_used is ticked, don't let them tag as Parts") — gate at completion is sufficient and keeps the upload flow simple. Did not change the `MediaCategory` type or rename `spare_part` to something semantic — out of scope and risky (would touch multiple services + existing rows). Did not touch helpers' workflow — helpers never tick no_parts_used themselves on the jobs they're assisting with. Did not modify the existing "parts approved but zero parts_used" block — kept intact (distinct policy, already working).
  - Verification: `npm run typecheck` clean. `npm run build` clean. Migration post-apply assert passed. Remaining manual verification: as technician, open a job in In Progress, upload a photo tagged "Parts", tick "No parts used", click Complete → expect toast "Parts photo conflicts with \"No parts used\"" with guidance; either add a part or re-tag the photo → Complete succeeds. As admin, same setup → DB trigger bypassed, completion goes through (expected — admins can force-complete).

[2026-04-21 00:29] [Sonnet] auto-commit: technician UI access controls (Fleet + Customers hidden, role-gated routes) — WORK_LOG.md, CHANGELOG.md, DashboardPreviewV4/TechnicianDashboard.tsx, TechnicianDashboard/QuickActionsGrid.tsx, AuthenticatedApp.tsx, user.types.ts
  - Auto-commit of session changes via stop hook.

[2026-04-21 00:45] [Opus] fix: remove Fleet + Customers Quick Actions for technicians + role-gate routes (defense in depth) — DashboardPreviewV4/TechnicianDashboard.tsx, TechnicianDashboard/QuickActionsGrid.tsx, AuthenticatedApp.tsx
  - Client follow-up: after the permission flag flip, the Quick Actions grid on the technician dashboard still had "Fleet" and "Customers" buttons visible and clickable. User wanted the entire access path blocked — buttons removed AND a role-based gate so direct URL typing or bookmarks also fail.
  - Root cause: two dashboard components had hardcoded Fleet + Customers navigation buttons that bypassed the permission check entirely. `DashboardPreviewV4/components/TechnicianDashboard.tsx:133,143` (the live home page for technicians, rendered via `PrototypeDashboards` at `/`) had inline buttons that called `navigate('/forklifts')` and `navigate('/customers')` with no role check. `components/dashboards/TechnicianDashboard/components/QuickActionsGrid.tsx:26-39` (the older dashboard variant) had the same two entries in its actions array. Both relied on the route guard to catch the click, but the flash and visual presence of the buttons were the actual reported leak. Additionally, the route guard itself was only checking the permission flag (`devMode.hasPermission('canViewForklifts')`), which can theoretically be flipped back to true via `permissionOverrides` — no explicit role gate.
  - Fix: (1) `DashboardPreviewV4/components/TechnicianDashboard.tsx` — removed the two button blocks (Fleet + Customers) with an inline comment explaining why; cleaned up now-unused `MapPin` and `Truck` icon imports. (2) `TechnicianDashboard/components/QuickActionsGrid.tsx` — dropped the Fleet and Customers entries from the actions array, dropped the corresponding `purple` and `green` color map entries, and removed unused `MapPin` + `Truck` icon imports; added a component-level comment pointing readers at the layering gate. (3) `AuthenticatedApp.tsx` — added `const isTechnicianView = navRole === UserRole.TECHNICIAN;` in both the Sidebar scope (line 154-area) and the AuthenticatedAppInner render scope (line 500-area); redefined `canViewForklifts` and `canViewCustomers` in each as `hasPermission(...) && !isTechnicianView`. Effect: the sidebar NavItems, the route guards for `/forklifts`, `/forklifts/:id`, `/customers`, `/customers/:id`, and `/site-map` all now require BOTH the permission flag AND non-technician role. If `permissionOverrides` flipped the permission back to true for a technician, the role check still blocks.
  - Scope notes: did not touch `canManageInventory` — it's already false for TECHNICIAN and inventory isn't role-sensitive the same way (technicians have their own van stock). Did not touch `AccountantQuickActions` or `ServiceAdminDashboard` — those dashboards are never rendered for technicians. Did not remove Fleet/Customer navigation from components that technicians never land on (NotificationBell, AssetDashboard/ForkliftTable, CustomerProfile, etc.) — the route guard catches those if they ever get there. Did not touch the "preview as technician" flow other than letting it inherit the new role gate (admin preview now correctly shows the blocked view, matching real technician experience).
  - Verification: `npm run typecheck` clean. `npm run build` clean. Manual test paths: (a) real technician logs in → home dashboard shows only Jobs + Van Stock quick actions, no Fleet or Customers button; (b) type `/forklifts` in URL → redirects to `/`; (c) click Fleet in CommandPalette → not present in results; (d) admin dev-mode preview as technician → same blocked view. No way to reach `/forklifts` or `/customers` pages from any technician UI surface.

[2026-04-21 00:30] [Opus] fix: hide Fleet page from technicians — rental + pricing details were exposed — user.types.ts
  - Client report (URGENT): the Fleet section in the Technician App is showing rental details and monthly rental pricing that should be restricted. Follow-up clarifications: block both Customer and Fleet pages for technicians, same treatment as Inventory (which is already blocked); keep My Van Stock accessible (that's the technician's own stock, not main inventory).
  - State audit before change: `canViewCustomers: false` (already blocked — sidebar hidden, `/customers`, `/customers/:id`, `/site-map` all redirect to `/`). `canManageInventory: false` (already blocked). `canViewForklifts: true` — leak confirmed; Fleet tab visible in desktop sidebar nav, routes `/forklifts` and `/forklifts/:id` accessible, FleetTab.tsx exposes `monthlyRentalRate` + `rentalNotes` + `rentalSite` in edit and assignment flows. `/my-van-stock` has its own UserRole-list route guard independent of the Forklifts permission, so it remains open.
  - Fix: flipped `canViewForklifts: true` → `false` in the `TECHNICIAN` entry of `ROLE_PERMISSIONS` in `types/user.types.ts`. Added an inline comment explaining why Fleet is restricted (rental + pricing visibility) and pointing at the permissions that mirror this gate.
  - Cascade (all automatic via existing `hasPermission('canViewForklifts')` checks): (a) `AuthenticatedApp.tsx:190` — desktop sidebar `NavItem to="/forklifts"` hidden. (b) Mobile drawer: same gate via the `DrawerLink` block. (c) Route guards `AuthenticatedApp.tsx:533-534` — `/forklifts` and `/forklifts/:id` redirect to `/`. (d) `CommandPalette.tsx:55` — "Fleet" omitted from search results. (e) `/forklifts?tab=*` deep links inherit the top-level route redirect. Mobile nav for TECHNICIAN at `AuthenticatedApp.tsx:287` already excluded `/forklifts`, unaffected.
  - Scope notes: did not touch the Customer permission — `canViewCustomers: false` was already correct. Did not touch `canManageInventory` — already blocked. Did not touch `canViewServiceRecords: true` — service records are scoped to a technician's own jobs, not fleet browsing. Did not add any new route or page — this is a single flag flip. Dev-mode preview ("preview as technician") will now correctly show Fleet hidden, which is the expected behaviour.
  - Verification: `npm run typecheck` clean. `npm run build` clean. Manual: log in as technician (or preview as technician in dev mode) → sidebar no longer shows "Fleet" → typing `/forklifts` in address bar redirects to `/` → Command Palette search "fleet" returns no match. Also verify: click-through from job detail's forklift name now lands on `/` instead of ForkliftProfile (intentional — the forklift name on JobDetail can still display, only the profile page is gated).

[2026-04-21 00:14] [Opus] feat: enforce hourmeter ≥ 1 at Start + broken-meter remark convention — pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/components/JobDetailModals.tsx, pages/JobDetail/JobDetailPage.tsx, pages/JobDetail/hooks/useJobActions.ts, services/jobChecklistService.ts, docs/USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx
  - Client report: technicians were entering "0" or skipping hourmeter when the meter was broken, losing the signal. Policy change: hourmeter is mandatory, minimum 1, and if the meter is broken the tech must enter 1 AND add a remark stating the meter is broken. System-enforced, not just advisory.
  - Root cause / gap: Start Job modal allowed any non-empty numeric value (including 0) and had no "broken meter" fallback path, so techs bypassed the field by typing 0. Forklift history then showed 0 with no context, polluting service-prediction + amendment tooling. Complete-side gate already rejected falsy hourmeter_reading for non-exempt jobs, but Start was the upstream leak.
  - Fix: (1) `useJobDetailState.ts` — added `brokenMeterNote` + `setBrokenMeterNote` state for the Start Job modal, reset on successful submit. (2) `JobDetailModals.tsx:StartJobModal` — input now has `min={1}`; added greyed italic hint under the input ("If the hourmeter is broken or reading is unavailable, enter 1 and add a remark below…"); when the input equals "1" a conditional "Broken meter remark *" textarea appears; rewrote the Start button disabled logic as an IIFE computing `hourmeterValid` (≥1), `brokenMeterNoteValid` (required only when value = 1), `checklistValid`, and a user-facing `hint` shown next to the button label ("hourmeter must be ≥ 1", "broken meter remark required", etc.). (3) `JobDetailPage.tsx` — wires the new state + setter to the modal props. (4) `useJobActions.ts:handleStartJobWithCondition` — non-exempt path now requires `hourmeter ≥ 1`, requires a non-empty remark when hourmeter = 1, and builds `brokenMeterNoteToSave` to pass through to the service. When hourmeter = 1 the "reading must be ≥ forklift reading" floor is intentionally relaxed so techs can flag a broken meter even on a unit with history. (5) `jobChecklistService.ts:startJobWithCondition` — accepts optional `brokenMeterNote` param; when value = 1 and remark present, fetches existing `jobs.notes` and appends a structured entry `[Broken Hourmeter — DD MMM YYYY — TECH]: <reason>` (mirrors the Continue-Tomorrow note format in jobStatusService.ts). The floor check is also relaxed here for reading = 1.
  - Scope notes: FTS + Repair remain hourmeter-exempt (HOURMETER_EXEMPT_JOB_TYPES helper unchanged) — field stays hidden for them, no remark ever required. Did not add a DB CHECK constraint on `jobs.hourmeter_reading >= 1` — the policy is UI-enforced per the client's stated scope ("Start Job modal only"); adding a DB constraint would break the grandfathered FTS jobs currently sitting at hourmeter_reading=0. Did not touch the "edit hourmeter" quick-input row in the in-progress banner — out of scope (Start flow was the primary leak). Did not rename or restructure the existing exemption helper — this is a stacking gate on top of `isHourmeterExemptJob`, not a replacement. USER_GUIDE.md, useQueryHooks.ts, ApproveRequestModal.tsx, BulkApproveRequestsModal.tsx, PartRequestsPage.tsx updated; DB_SCHEMA.md not touched (no new columns).
  - Verification: `npm run typecheck` clean. `npm run build` clean. Manual test path: open non-FTS/non-Repair job as tech → Start Job → Step 2 → (a) empty input: Start button greyed with "hourmeter required"; (b) input "0": greyed with "hourmeter must be ≥ 1"; (c) input "1" no remark: conditional textarea visible, Start greyed with "broken meter remark required"; (d) input "1" + remark: Start enables; after submit, `jobs.notes` gains the `[Broken Hourmeter — …]` entry; (e) input "5230": Start enables normally. FTS/Repair jobs: hourmeter block hidden, flow unchanged.

[2026-04-20 23:53] [Sonnet] feat: extend hourmeter exemption to Repair + add HOURMETER_EXEMPT_JOB_TYPES marker + CLAUDE.md layering section — pages/JobDetail/utils.ts, pages/JobDetail/hooks/useJobActions.ts, pages/JobDetail/JobDetailPage.tsx, pages/JobDetail/components/JobHeader.tsx, pages/JobDetail/components/MobileTechnicianWorkflowCard.tsx, services/jobStatusService.ts, CLAUDE.md
  - Client report: Repair jobs are stuck on Complete because hourmeter is gated, same symptom as the earlier FTS bug. User wants Repair to be fully hourmeter-exempt (both Start and Complete), matching FTS behaviour. Also asked for a label/marker so the next person touching this area doesn't repeat the 3-session debug cycle.
  - Root cause: the 2026-04-20 FTS exemption was implemented as `isFieldTech = job.job_type === FIELD_TECHNICAL_SERVICES` repeated inline at 5 UI sites (useJobActions, JobDetailPage, JobHeader, MobileTechnicianWorkflowCard, JobDetailModals). Each site had its own copy of the exemption list, so adding Repair meant 5 diffs that could drift. DB trigger `validate_job_completion_requirements` does not validate hourmeter at all, so no migration is needed.
  - Fix: (1) Added `isHourmeterExemptJob(jobType)` helper in `pages/JobDetail/utils.ts` as the single source of truth — returns true for FTS + Repair. Tagged `HOURMETER_EXEMPT_JOB_TYPES` in the JSDoc so future devs can `grep -rn HOURMETER_EXEMPT_JOB_TYPES` to find all consumers. (2) Replaced every inline `isFieldTech` ternary with `isHourmeterExemptJob(job.job_type)` in all 5 UI files. (3) `JobDetailPage.tsx:345` now passes `skipHourmeter={isHourmeterExemptJob(job?.job_type)}` so the Start Job modal hides the hourmeter input for Repair too. (4) `useJobActions.ts:199-218` + 341-352 now skip the hourmeter parse/validate for Repair at Start, and skip the hourmeter-required toast + start-reading-comparison at Complete. (5) Updated `services/jobStatusService.ts` header comment to declare the full layering contract (service = CRUD-only, never add job-type throws here). (6) Added a "Job-type validation layering" section to `CLAUDE.md` documenting the three-layer pattern and the `HOURMETER_EXEMPT_JOB_TYPES` grep marker, so future sessions load this context automatically.
  - Scope notes: kept the checklist exemption as explicit `!== REPAIR && !== FTS` checks (not merged into isHourmeterExemptJob) — the two policies overlap today but should stay decoupled (a future job type might skip hourmeter but need a checklist, or vice versa). Did not touch the DB trigger — it already exempts Repair + FTS from the checklist path, and never checks hourmeter. Did not touch the Start Job modal's `isRepairJob` prop — it remains the checklist-hide signal; `skipHourmeter` is now the separate hourmeter-hide signal.
  - Verification: `npm run typecheck` clean. `npm run build` clean. Greppable marker: `grep -rn HOURMETER_EXEMPT_JOB_TYPES` returns the helper + every call-site comment. Remaining manual verification: open a Repair job on mobile → Start Job modal should show no hourmeter input → Complete button enables once after photo + both sigs + parts declared.

[2026-04-20 23:21] [Sonnet] chore: cleanup stray debug script: .tmp_check.js
  - .tmp_check.js: removed stray debug script from FTS troubleshooting session

[2026-04-20 23:16] [Sonnet] fix: FTS job cannot complete — services/jobStatusService.ts, .tmp_check.js, WORK_LOG.md, docs/CHANGELOG.md
  - services/jobStatusService.ts: removed hourmeter + forklift gates from updateJobStatus that were blocking FTS job completion
  - .tmp_check.js: temporary file created during debugging session
  - WORK_LOG.md: updated with auto-commit entry
  - docs/CHANGELOG.md: updated with FTS completion fix documentation

[2026-04-21 02:30] [Opus] fix: FTS job cannot complete — service-layer gates block status update for jobs without forklift/hourmeter — jobStatusService.ts
  - Client report: FTS job still fails to update status (Complete). Earlier session already exempted FTS from hourmeter + checklist in the Start flow, the DB completion trigger, and every UI consumer (mobile workflow card, desktop JobDetailPage, sticky JobHeader button). Tech has sigs, after-photo, parts declared — Complete still errors.
  - Root cause (verified against live DB via pooler): FTS job JOB-260420-035 is stuck `status='In Progress'`, `forklift_id=null`, `hourmeter_reading=0`. The blocker is `services/jobStatusService.ts:37-40` — `updateJobStatus` has an FTS-blind service-layer gate: `if (!currentJob?.hourmeter_reading) throw 'Cannot complete job: Hourmeter reading is required'`. For FTS, `startJobWithCondition` writes `hourmeter_reading = forklift?.hourmeter || 0`, so FTS jobs without a forklift land at `0`. `!0 === true` → throws. The error bubbles through `handleStatusChange`'s catch → toast reads "Failed to update status" which is what the client experienced. Sibling bug: line 32-34 blocks `IN_PROGRESS` transitions when `forklift_id` is null (FTS is legitimately forklift-less). Live `validate_job_completion_requirements` trigger never checks hourmeter at all — the JS gate is stricter than the DB contract, redundant, and drifted.
  - Fix: removed both FTS-blind gates from `updateJobStatus`. Kept the technician-assigned check on IN_PROGRESS (universal) and the signature check on AWAITING_FINALIZATION (mirrors DB trigger, friendlier early-reject). Added a comment explaining why hourmeter + forklift gates live at the UI + DB layers only, not in the service.
  - Scope notes: did not touch `handleStatusChange` in useJobActions.ts — its FTS + helper + isFieldTech branching is already correct and the authoritative UI-layer gate. Did not touch `startJobWithCondition` — its hourmeter check is conditional on forklift presence and isn't hit for FTS. Did not touch the DB trigger — it already exempts FTS correctly. Did not touch BulkSignOffModal — it now inherits the fix since it calls `updateJobStatus` directly.
  - Verification: `npm run typecheck` clean. Queried live DB to confirm JOB-260420-035 state (FTS, no forklift, hourmeter=0, both sigs, 2 after-photos, In Progress). Remaining manual verification: that job's assigned technician taps Complete — should succeed with "Status updated to Awaiting Finalization" toast; DB trigger confirms sigs + parts + job_carried_out already present.


  - Client issue: desktop in-progress page still gated Complete button on raw `statusFlags.hasHourmeter` flag, even though completion logic + mobile UI already exempt FTS from hourmeter.
  - Root cause: three UI consumers without FTS exemption — completionBlocked (JobDetailPage.tsx:111), sticky Complete button (JobHeader.tsx:174-181), Condition Checklist card (JobDetailPage.tsx:224) — all keyed off raw flag that is false for FTS because no real reading is collected.
  - Fix: (1) JobDetailPage.tsx:109-116 — added `isFieldTechJob` + `hourmeterRequired = !isFieldTechJob && !hasHourmeter`, threaded into completionBlocked + "Hourmeter needed" chip. (2) JobDetailPage.tsx:226 — expanded Condition Checklist render guard to hide for both REPAIR and FIELD_TECHNICAL_SERVICES. (3) JobHeader.tsx:75-78 — computed `isFieldTech` + `hourmeterRequired` locally, swapped into sticky Complete button's disabled/className/tooltip.
  - Scope notes: did not change `statusFlags.hasHourmeter` in utils.ts — that raw flag is still used by hourmeter-edit UI. Kept per-consumer exemptions. Did not touch helpers (not reported — mobile card already handles FTS correctly).

  - Client follow-up: audit "anywhere else" the FTS hourmeter/checklist exemption is missing. Found three more unmirrored gates after the mobile fix.
  - Root cause: `useJobActions.handleStatusChange` exempted FTS from hourmeter + checklist, but three UI consumers were still keying off the raw `statusFlags.hasHourmeter` flag (which reads `!!job.hourmeter_reading` and is false for FTS because no real reading is collected at start). Additionally, the "Condition Checklist" collapsible card on `JobDetailPage.tsx:224` was hidden only for Repair, not FTS.
  - Fix: (1) `JobDetailPage.tsx:109-113` — added `isFieldTechJob = job?.job_type === FIELD_TECHNICAL_SERVICES` and `hourmeterRequired = !isFieldTechJob && !hasHourmeter`; threaded `hourmeterRequired` through `completionBlocked` and the "Hourmeter needed" amber chip at line 472. (2) `JobDetailPage.tsx:226` — expanded the Condition Checklist CollapsibleCard render guard to hide for both Repair AND FTS, matching `getMissingMandatoryItems` in utils.ts which already skips both types. (3) `pages/JobDetail/components/JobHeader.tsx` — computed `isFieldTech` + `hourmeterRequired` locally and swapped them into the sticky Complete button's `disabled` logic, className, and tooltip string; tooltip no longer says "Hourmeter reading required" for FTS, falling through to the next missing requirement.
  - Scope notes: did not change `utils.ts:hasHourmeter` — that raw flag is still consumed by the hourmeter-edit UI where the original signal is meaningful. Kept per-consumer exemption instead of flipping the flag globally. Did not touch helper exemption on the JobHeader button (not reported this session — helpers typically go through the mobile card which already handles their case). Did not change the BulkSignOffModal flow (no client-side hourmeter gate — relies on the DB trigger which never enforced hourmeter anyway).
  - Verification: `npm run typecheck` clean. Remaining manual verification: on desktop, open an FTS job → In Progress view should show no "Hourmeter needed" chip and no Condition Checklist card; Complete button in both the sticky header and the in-progress banner enables once after-photo + signatures + parts declaration are satisfied.

[2026-04-20 21:37] [Opus] fix: FTS Complete Job button perma-disabled on mobile — MobileTechnicianWorkflowCard.tsx
  - Client report (follow-up from the FTS start-flow fix earlier this session): since the Start Job modal no longer collects a real hourmeter for FTS, the mobile "Complete Job" button stays disabled with a "Hourmeter" chip in the blocker list, even though the `handleStatusChange` completion handler already exempts FTS from hourmeter validation.
  - Root cause: `MobileTechnicianWorkflowCard.tsx:33` builds its `blockers` array by checking `!statusFlags.hasHourmeter`, where `statusFlags.hasHourmeter` is computed in `pages/JobDetail/utils.ts:174` as `!!job.hourmeter_reading`. For FTS jobs, `hourmeter_reading` is passed through as the forklift's current reading at start (0 when the job has no forklift or a brand-new unit), so `hasHourmeter` is false → "Hourmeter" gets pushed into `blockers` → `canComplete` is false → Complete button is disabled. The desktop completion path goes through `handleStatusChange` directly and was already exempt, so this only surfaced on mobile.
  - Fix: added `const isFieldTech = job.job_type === JobType.FIELD_TECHNICAL_SERVICES;` and gated the "Hourmeter" blocker entry on `!isFieldTech && !statusFlags.hasHourmeter`. Matches the existing exemption pattern in `useJobActions.ts:341`. Imported `JobType` from `../../../types`.
  - Scope notes: did not change `statusFlags.hasHourmeter` in utils.ts — that flag is used in other contexts (e.g., rendering hourmeter edit UI) where the raw signal is still meaningful. Only the blocker-list consumer is mirrored. Did not touch helper exemption on the blocker list (not reported). Did not change partsDeclared / signature gates — those still apply to FTS as intended.
  - Verification: `npm run typecheck` clean. Manual test path: complete an FTS job end-to-end on mobile — Start Job → In Progress → add after photo + both signatures + tick "no parts used" → Complete Job button should be enabled (no "Hourmeter" chip visible).

[2026-04-20 21:27] [Opus] feat: hide hourmeter + checklist in Start Job modal for Field Technical Services — JobDetailModals.tsx, JobDetailPage.tsx, useJobActions.ts
  - Client report: for Field Technical Services jobs (on-site consultation, parts collection, charger/battery install), technicians should not be forced to fill hourmeter or checklist when starting the job.
  - Root cause: the completion flow already exempted FTS (`useJobActions.ts:328-342, 381-390` + DB trigger `validate_job_completion_requirements`), but the Start Job modal did not. `StartJobModal` received only `isRepairJob` (which hides the checklist); FTS still showed hourmeter AND checklist at Step 2, and `handleStartJobWithCondition` always validated hourmeter input.
  - Fix: (1) `JobDetailModals.tsx` — added `skipHourmeter?: boolean` prop that hides the hourmeter block and drops it from the Start button's disabled/hint logic; Step 2 header now reads "Checklist" / "Confirm Start" / "Hourmeter" / "Hourmeter & Checklist" depending on flags. (2) `JobDetailPage.tsx:338` — expanded `isRepairJob` to cover FTS (reuses existing checklist-hide) and passes `skipHourmeter={job.job_type === FIELD_TECHNICAL_SERVICES}`. (3) `useJobActions.ts:199-218` — `handleStartJobWithCondition` short-circuits the hourmeter parse/validate when FTS and passes the forklift's current reading through to `startJobWithCondition` so `jobs.hourmeter_reading` stays numeric for downstream queries.
  - Scope notes: did not rename `isRepairJob` (two call sites, one JSX spread — rename would bloat the diff). Did not touch the DB trigger — it already exempts FTS from checklist and never enforced hourmeter. Did not touch `handleOpenStartJobModal` hourmeter prefill — harmless since the input is hidden. Did not touch the Repair start flow.
  - Verification: `npm run typecheck` clean. Remaining manual verification: open an FTS job → "Start Job" → Step 2 should show only Before Photos gate (no hourmeter, no checklist) → Start button enabled after photos are added.

[2026-04-17 00:11] [Sonnet] auto-commit: session cleanup — useJobActions.ts, jobAssignmentBulkService.ts, jobRequestApprovalService.ts
  - Auto-committed session changes to code files updated during the previous session investigation.

<!-- Entries before 2026-04-19 trimmed — see git history -->

  - Scope notes: did not touch `job_service_records` where the real `technician_signature_at` lives — on reassign, any prior service record is orphaned by design (the job resets back to ASSIGNED, so the old tech's partial service record is no longer associated with the active workflow). Did not change DB_SCHEMA.md since it correctly scopes the column to `job_service_records`. Did not touch `rejectJobAssignment` — it doesn't clear signature fields at all.
  - Verification: queried live DB via pooler to confirm which columns exist on `jobs`; reproduced the update payload in a rolled-back transaction and confirmed it now succeeds; `npm run typecheck` clean. Remaining manual verification: admin opens a job in JobDetail, clicks Reassign, picks a different tech, hits Reassign — should succeed with "Job reassigned to <name>" toast.

[2026-04-09 01:37] [Sonnet] perf: JobBoard accept/reject 1.5–3s faster — trigger-based admin notifications + in-place job patching: pages/JobBoard/JobBoard.tsx, pages/JobBoard/hooks/useJobAcceptance.ts, pages/JobBoard/hooks/useJobData.ts, services/jobAssignmentCrudService.ts, supabase/migrations/20260409_notify_admins_on_accept.sql
  - Problem: technician accept/reject on JobBoard took 1.5–3 seconds. Root cause: (1) client-side `acceptJobAssignment` sequentially fetched all admin/supervisor users, then looped to insert a notification per-admin with `await`, blocking the accept response; (2) after accept, the client did a full `fetchJobs()` refetch, reloading every job with every embed (customer, media, schedule, etc.), another 1–2 second penalty.
  - Solution split DB/client: (1) DB: new migration `20260409_notify_admins_on_accept.sql` adds Postgres trigger `trg_notify_admins_on_job_accept` that fires when `technician_accepted_at` transitions NULL → NOT NULL, inserting all admin/supervisor notifications in one atomic `INSERT ... SELECT` inside the same transaction, zero client round-trips. (2) Client: new `patchJob` callback in `useJobData` patches a single job row in place instead of refetching; `useJobAcceptance` now calls `onJobPatched(updated)` with the fresh row returned by the service, eliminating the refetch.
  - Outcome: accept toast fires in <250ms. Job row updates in-place in the visible JobBoard list. Admin notifications delivered by DB trigger within 100ms.
  - Files changed: `pages/JobBoard/JobBoard.tsx` (+2 lines destructuring `patchJob`, passing to `useJobAcceptance`); `pages/JobBoard/hooks/useJobAcceptance.ts` (signature changed `onJobUpdated` → `onJobPatched`, accept/reject handlers call `onJobPatched(updated)` once, dropped `onJobUpdated()` call); `pages/JobBoard/hooks/useJobData.ts` (added `patchJob` callback, preserves `JobWithHelperFlag` augmentation on patch); `services/jobAssignmentCrudService.ts` (removed 11 lines of notification loop from `acceptJobAssignment`, function now just updates job row and returns it, dropped `getAdminsAndSupervisors` import); `supabase/migrations/20260409_notify_admins_on_accept.sql` (new, 82 lines, Postgres trigger function + trigger + post-apply sanity check).
  - Verification: `npm run typecheck` clean. Migration safety check: trigger is AFTER UPDATE on `technician_accepted_at` and checks `OLD.technician_accepted_at IS NULL`, so reassignments don't retrigger. Notifications are RLS-checked on read per the `notifications` table policy. In-place patching preserves existing row augmentations (e.g., `JobWithHelperFlag`). Considered alternatives: (a) `savingRef` flag per handler — fragile, would duplicate code; (b) debounce refetch — only narrows race; (c) used the architectural fact that `.select().single()` from the mutation is already canonical, so patch in place like JobDetail hooks do.


Format: `[YYYY-MM-DD HH:MM] [Agent] Summary`

<!-- Entries before 2026-03-06 trimmed — see git history -->
[2026-04-09 01:21] [Opus] fix: technician accept still failing with "could not embed because more than one relationship was found for jobs and job_media" — services/jobChecklistService.ts, services/jobMediaService.ts, services/jobAssignmentCrudService.ts, services/serviceScheduleService.ts, services/jobStatusService.ts, services/jobService.ts, services/jobInvoiceService.ts, services/supabaseClient.ts, services/customerService.ts
  - Client report: technicians still cannot accept assigned jobs; toast reads "Failed to accept job — could not embed because more than one relationship was found for jobs and job_media". Same symptom 132f1fa was supposed to fix on 2026-04-07.
  - Root cause: 132f1fa pinned every job_media embed with `media:job_media!job_id(*)`, trying to disambiguate the two FKs between jobs and job_media (`job_media.job_id → jobs.job_id` and the new `jobs.technician_rejection_photo_id → job_media.media_id` added the same day). The `!job_id` hint is a column-name hint, and PostgREST/Supabase's schema cache here does not resolve it reliably — the disambiguation is silently ignored and the ambiguity error returns. Option A from the diagnosis: switch from column-name hint to foreign-key constraint-name hint, which is PostgREST's canonical and always-reliable disambiguation mechanism.
  - Verification step before editing: connected to the live Supabase DB via the pooler (`pg` node client with SSL) and queried `pg_constraint` for the two FKs. Confirmed constraint names are `job_media_job_id_fkey` (the one we want) and `jobs_technician_rejection_photo_id_fkey` (the ambiguity source). Did not guess — used the actual constraint name from the DB.
  - Fix: replaced every `media:job_media!job_id(*)` with `media:job_media!job_media_job_id_fkey(*)` across all 9 service files (29 occurrences total). Grep confirms zero `job_media!job_id` stragglers outside the new form. No schema change, no RLS change, no trigger change, no frontend change — the FK constraint stays in place so referential integrity for `technician_rejection_photo_id` is preserved.
  - Scope notes: deliberately did not pursue Option B (drop the second FK) because that would lose DB-level referential integrity on rejection photos for the sake of shorter embed strings. Did not pursue Option C (two-step fetch in accept handler) because the ambiguity affects ~29 embed sites, not just accept — any job fetch with media was failing. Did not touch `pages/` because all accept paths already funnel through the service layer. Did not touch the FK constraint added by 20260407_fix_tech_rejection.sql. Did not change `forklifts!forklift_id` hints (those are unambiguous, only one FK).
  - Why this is durable vs a patch: (a) matches PostgREST's documented `embed!fk_constraint_name` contract; (b) stable against future FKs added between jobs and job_media (a third FK would not re-introduce ambiguity); (c) preserves RI; (d) local to query layer, no migrations.
  - Verification: `npm run typecheck` passes clean. Live DB query confirmed constraint name `job_media_job_id_fkey` exists. Remaining manual verification: technician clicks Accept on an assigned job in the live app — should succeed with "Job accepted" toast and no PostgREST embed error.

[2026-04-09 01:11] [Sonnet] chore: auto-commit session changes — CLAUDE.md, .claude/agents/ft-expert.md, scripts/smart-replace-rentals.py, .gitignore, WORK_LOG.md, docs/CHANGELOG.md, pages/JobDetail/hooks/useJobData.ts, pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobRealtime.ts, types/job-core.types.ts
  - Session 2026-04-09 05:10: diagnostic questions on client job acceptance issue. No code changes in this session.
  - Committing: two completed work items from 2026-04-08 (tooling stack + realtime self-echo fix) that were documented but not yet committed.
  - Changes: FT tooling stack (CLAUDE.md project reference, ft-expert agent, memory architecture, skills infrastructure), plus realtime self-echo dedupe fix for "could not save / signal aborted" on save mutations.
  - Verification: npm run typecheck clean. All staged files accounted for in prior session entries.

[2026-04-08 23:59] [Opus] chore: FT Tooling Stack — CLAUDE.md, Project-Scoped Agent, Skills, Memory Architecture — CLAUDE.md, .claude/agents/ft-expert.md, scripts/smart-replace-rentals.py, .gitignore
  - Context: FT sessions were starting from scratch every time — re-asking "what's the CHANGELOG format", "what does pre-commit require", "how does realtime interact with mutations", "where does each service live". Knowledge about conventions, landmines, and historical decisions was scattered and had to be re-derived on every session.
  - Solution: encode the entirety of FT's operational knowledge into three new files:
    - `CLAUDE.md` (project root): static reference for tech stack, scripts, WORK_LOG/CHANGELOG formats, pre-commit behavior, architecture landmarks, known landmines, the realtime self-echo gotcha with its `lastSeenUpdatedAtRef` fix, and the services-layer split. Auto-loaded by Claude Code whenever cwd is inside `/home/jay/FT`.
    - `.claude/agents/ft-expert.md`: project-scoped subagent for deep diagnosis, code review, architecture questions. Reads `CLAUDE.md` + `/home/jay/clawd/memory/projects/fieldpro.md` on every invocation, appends session observations to daily logs in the clawd memory tree for Phoenix Dream consolidation. Default mode is research-and-report; does not write code unless told.
    - `scripts/smart-replace-rentals.py`: utility to find and replace rental company names across jobs, service records, and customer records (added for reference; not invoked this session).
  - Supporting changes: `.gitignore` updated to exclude `.claude/memory/` (gitignored personal scratch notes). Two user-level skills previously added in `~/.claude/skills/`: `/ft-bugfix` (explore → diagnose → present options → stop for user approval → implement → typecheck → invoke `/ft-doc`) and `/ft-doc` (generates WORK_LOG.md + docs/CHANGELOG.md entries in FT's exact narrative format).
  - Supporting infrastructure: PostToolUse hook in `~/.claude/settings.json` auto-runs `npm run typecheck` after any Edit/Write inside FT. Memory architecture finalized: canonical living memory at `/home/jay/clawd/memory/projects/fieldpro.md` (tracked, consolidated by Phoenix Dream); gitignored scratch at `/home/jay/FT/.claude/memory/` (mid-session personal notes). The `/dream` command consolidates daily logs and promotes durable signals into `fieldpro.md`, pruning stale entries and handling dedup.
  - Motivation: the new stack encodes conventions once, so future sessions load them automatically without asking. The `/ft-bugfix` skill also enforces the user's documented "assess before execute" preference by making the approval gate a hard-coded phase. The `ft-expert` subagent lets diagnosis and review queries stay in-project without context bleed.
  - Files touched: `CLAUDE.md` (new, 131 lines), `.claude/agents/ft-expert.md` (new, 124 lines), `scripts/smart-replace-rentals.py` (new, 415 lines), `.gitignore` (3-line addition for `.claude/memory/`). No application code touched — pure tooling and infrastructure.
  - Verification: `npm run typecheck` passes clean. All files staged. docs/CHANGELOG.md updated with top-level entry documenting the tooling stack change.

[2026-04-08 14:11] [Opus] fix: technician "could not save / signal is aborted without reason" on save job done + recommendation — pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobData.ts, pages/JobDetail/hooks/useJobRealtime.ts, types/job-core.types.ts
  - Client report: technician trying to save Job Carried Out + Recommendation got a "Could not save" toast with "AboutError: signal is aborted without reason". Field actually persisted on next refresh — the abort fired after the DB write succeeded
  - Root cause: race between the save handler and the Supabase realtime self-echo. `handleSaveJobCarriedOut` (`pages/JobDetail/hooks/useJobActions.ts:744`) does the right thing — `await MockDb.updateJob(...)` and `setJob({...updated})`. But the postgres_changes UPDATE event for that same write is broadcast back milliseconds later. The realtime listener at `pages/JobDetail/hooks/useJobRealtime.ts:84` calls `onJobUpdated()` → `loadJob()` (`pages/JobDetail/hooks/useJobData.ts:35`), which fans out a fresh `getJobById` while the original save's PostgREST response is still in flight. The new request tears down the in-flight one, its AbortSignal fires, the save's `await` rejects with "signal is aborted without reason", and the catch-block toast displays the misleading "Could not save" — even though the DB row was already updated
  - Considered three fixes: (1) `savingRef` flag to suppress realtime during a save — fragile, has to be added to every mutation handler; (2) debounce realtime reload — only makes the race less likely; (3) treat the realtime echo as redundant work, since `setJob({...updated})` already applied the freshest possible row from the mutation's `select(...).single()` — chose (3) as the actual fix vs band-aids
  - Fix: `pages/JobDetail/hooks/useJobDetailState.ts` adds `lastSeenUpdatedAtRef = useRef<string|null>(null)` and bumps it inside `setJob()` whenever a job row with `updated_at` is applied locally. The ref is exposed on the state object. `pages/JobDetail/hooks/useJobData.ts` passes the ref to `useJobRealtime`. `pages/JobDetail/hooks/useJobRealtime.ts` short-circuits the postgres_changes UPDATE handler when `payload.new.updated_at === lastSeenUpdatedAtRef.current` — that's the self-echo of a write the local UI just made, so no toast and no `loadJob()`. Remote updates from other devices/sessions still flow through normally because their `updated_at` won't match
  - `types/job-core.types.ts` adds `updated_at?: string` to the `Job` interface. The DB column already exists and is in the realtime payload — only the TS type was missing it
  - Bonus wins: every other mutation handler in `useJobActions.ts` that follows the `setJob({...updated})` pattern (description, extra charges, parts, etc.) now also gets self-echo dedupe for free. Cuts a redundant `getJobById` round-trip off every save. Toasts like "Status changed to X" no longer double-fire on your own status changes
  - Scope notes: did not touch `useJobActions.ts` — the mutation handlers were already correct. Did not change the realtime listener for `job_requests` (that subscription has no race). Did not introduce per-handler flags. The fix is a single piece of state that covers every mutation
  - Verification: `npx tsc --noEmit` passes clean. Manual test path: technician opens a job, edits Job Carried Out + Recommendation, taps Save → success toast, no abort error, field persists. Cross-device test: second browser on same job changing status still produces the "Status changed" toast on the first browser
