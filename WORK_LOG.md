[2026-05-05 05:15] [Opus] fix: Van Stock — backfill liquid quantity → bulk_quantity, eliminate negative-bq + at-risk seeds, document price-diff table for Shin — supabase/migrations/20260504_van_stock_liquid_quantity_backfill.sql, supabase/migrations/20260504_parts_liquid_price_drift_view.sql, services/liquidInventoryService.ts, services/vanStockQueriesService.ts, pages/VanStockPage/components/modals/VanStockDetailModal.tsx, WORK_LOG.md, docs/CHANGELOG.md
  - Note: this single entry covers all the Van Stock work done in this session — earlier entries below describe the same files but their timestamps are outside the pre-commit 15-minute window. The other in-flight files (helper extension, modal Unit Price column, parts_liquid_price_drift view) are documented in the older entries; this entry adds the backfill migration on top of those.
  - Client request / context: After shipping the per-liter price helper earlier today (entry below), Shin asked to investigate the VFG 7238 SHELL RIMULA `bulk_quantity = -9` data bug AND check for similar issues across all 9 vans. I had access to the original 9 xlsx checklists in /home/jay/Downloads/Ft/ to use as ground truth.
  - Investigation findings (4 distinct issue classes):
    - **Issue 1 (the original target — VFG 7238 negative bq)**: HAFIZ used SHELL TELLUS 8L and SHELL RIMULA 9L on jobs across 4/27–4/29. Each `use_internal` movement subtracted from `bulk_quantity`, but the seed migration (20260424) had loaded `bulk_quantity = 0` (only `quantity` was populated). So bq drained 0 → -7, 0 → -9. Movement audit log is correct; running balance was meaningless because the starting state was wrong.
    - **Issue 2 (11 at-risk rows on 3 OTHER vans)**: BRK 3280, FA 8326, VFA 6286 (the 2026-05-04 imports) all have liquid SKUs with `quantity > 0` AND `bulk_quantity = 0`. Same time bomb — first job consuming any liquid would drive bq negative.
    - **Issue 3 (item presence drift)**: 5 SKUs in BASRI's VEW 9631 Excel that DB doesn't have; a few connector parts in DB that aren't on the Excel checklists; and a `23303-64010 B` (with whitespace) vs `23303-64010B` ambiguity affecting 8/9 vans where the tech carries both variants but DB has them as separate rows.
    - **Issue 4 (84 price mismatches across 18 unique parts)**: catalog `parts.cost_price` disagrees with Shin's Excel "Unit Price (RM)" column for 18 parts. Most are 1–10% drift; two outliers are 10× and 3×. Full table in CHANGELOG for Shin to review.
  - Fix shipped (Issues 1 + 2 — `supabase/migrations/20260504_van_stock_liquid_quantity_backfill.sql`): single SQL migration covering all 32 liquid rows (32 = 11 untouched + 18 duplicates + 1 with-movements + 2 negative). The migration uses a deterministic per-row heuristic to handle three different broken pre-states without ambiguity:
    - **State A** (qty>0, bq=0, no movements — 11 rows): bulk_quantity := quantity. Simple seed→bulk move.
    - **State B** (qty=N, bq=N, no movements — 18 rows): bulk_quantity := bq (qty was duplicate seed, discard). Detected via `|bq − sum(movements)| > 2`.
    - **State C** (qty=32, bq=24, sum_of_movements=-8 — 1 row, VEW 9631 S-01925): bulk_quantity := bq (started at qty, reduced by movements). Same `|bq − sum| > 2` detection.
    - **State D** (qty=20, bq=-7, sum=-7/-8 — 2 rows on VFG 7238): bulk_quantity := qty + bq. Detected via `|bq − sum(movements)| ≤ 2`. Tolerance of 2 covers the audit-log inconsistency (one duplicate `use_internal` row that didn't actually update the running balance).
    - All 32 rows additionally set `quantity = 0` so the dual-unit math doesn't double-count via the helper's `> 0` fallback.
  - Iterative debugging note: first migration attempt did blanket additive merge (`bulk := bulk + quantity`), which double-counted the 19 State-B/C rows where `bulk_quantity` was already populated. Reverted using a JSON dump of pre-migration state, then re-derived the deterministic heuristic by inspecting `inventory_movements` for each ambiguous row. Final v2 migration applies cleanly with post-condition assertion (raises EXCEPTION if any liquid row still has quantity>0).
  - Live-DB verification: 32 liquid rows backfilled. 0 residual `quantity > 0`. 0 `bulk_quantity < 0`. VFG 7238 SHELL TELLUS now correctly shows 13L (load 20L − usage 7L), SHELL RIMULA shows 23L (load 32L − usage 9L). Fleet total 48,808 → 48,757 (−51 from VFG 7238 now reporting actual current liters instead of the helper's `> 0` fallback to original load).
  - Issue 3 + 4 (item-presence and price drift) — NOT FIXED THIS COMMIT, just documented. Shin needs to confirm the canonical price for 18 SKUs (notably BTP29659 SIDE SHIFT SPRING at 11.00 vs 110.00, S-00961 BRAKE BULB 1016 [48V] at 7.00 vs 22.24) before any UPDATE migration ships. Full table embedded in CHANGELOG entry.
  - Migration template documented for future imports: a header comment block in the new migration spells out the rule for liquid SKUs ("INSERT with quantity=0, bulk_quantity=N — never put liquid stock in `quantity`"). Future van imports should follow this pattern; both 20260424 and 20260504 imports violated it and required this corrective backfill.
  - Scope notes: did NOT touch `parts.cost_price` for any SKU (Shin needs to pick canonical rate first); did NOT merge the `23303-64010` / `23303-64010 B` SKU variants (catalog-level decision, separate scope); did NOT add new SKUs to BASRI's VEW 9631 from the Excel-only items (need physical audit first); did NOT modify `services/jobInvoiceService.ts` invoicing math which still uses `cost_price` (different domain — sale price for invoicing vs cost-of-stock for van valuation). Did NOT amend the 20260424/20260504 import migrations themselves — they've been applied; the corrective migration documents the right pattern for future imports going forward.
  - Verification: `npm run typecheck` clean (no code changed). Migration applied to live DB; pre/post `RAISE NOTICE` confirms 32 rows fixed and post-condition holds.
  - Out of scope (follow-ups): (a) Shin to pick canonical rates for 18 price-drift parts → 1 small UPDATE migration; (b) BASRI VEW 9631 has the most quantity drift across the fleet (10 mismatched rows) and 5 missing SKUs — worth a separate physical audit; (c) catalog-level decision on whether `23303-64010` and `23303-64010B` should be merged or kept split; (d) optionally add an admin dashboard tile for the `parts_liquid_price_drift` view so future drift surfaces proactively.

[2026-05-04 16:47] [Opus] fix: Van Stock — extend liquid-price math to use last_purchase_cost_per_liter (matching Shin's Excel) + ship parts_liquid_price_drift audit view — services/liquidInventoryService.ts, services/vanStockQueriesService.ts, pages/VanStockPage/components/modals/VanStockDetailModal.tsx, supabase/migrations/20260504_parts_liquid_price_drift_view.sql, WORK_LOG.md, docs/CHANGELOG.md
  - Client report / context: After shipping the initial liquid-aware `computeVanStockItemValue` (earlier entry today), Shin's verification showed a remaining discrepancy. VEW8236 system showed RM 6,416.55 vs Excel RM 5,218.50 (Δ RM 1,198.05). Live-DB audit across all 9 active vans found Δ RM 20,190.24 fleet-wide. Root cause traced to a single broken parts row.
  - Root cause (catalog data, not code): liquid SKUs in `parts` have **two** price columns (`cost_price` and `last_purchase_cost_per_liter`) and the catalog allows them to disagree. For GRANTT ATF DEX-III [18L] (`part_code = S-01044`): `cost_price = 210` (interpreted as per-18L-drum) but `last_purchase_cost_per_liter = 11` (per L). 210 / 18 = 11.67 ≠ 11.00 — so even the two columns on the same row don't agree. The system was using `cost_price` × bulk-qty everywhere; Shin's Excel uses the per-liter rate × bulk-qty. Across the fleet there are 101 GRANTT drums × Δ RM 199 (210 − 11) = RM 20,099 of the RM 20,190 gap. The remaining ~RM 91 is sub-percent rounding on SHELL RIMULA where 9.86 vs 9.80 differs by 6¢. Confirmed via parts catalog snapshot — only one liquid SKU has >5% disagreement between the two columns.
  - Fix (services/liquidInventoryService.ts): extended the helper into three functions sharing the same `LineItem` shape:
    - `getVanStockItemUnitPrice(item)` — unit-rate selector. Non-liquid → `cost_price`. Liquid → prefer `last_purchase_cost_per_liter` when > 0, else `avg_cost_per_liter` when > 0, else fall back to `cost_price`. Matches Shin's Excel intent for liquids.
    - `getVanStockItemEffectiveQty(item)` — quantity selector. Non-liquid → `quantity`. Liquid → prefer dual-unit math `(containers × container_size) + bulk_qty` when > 0, falling back to `quantity` when both dual-unit fields are zero (covers legacy seed rows from the 20260424/20260504 van imports where stock counts went into `quantity` only). Uses `> 0` not `!== 0` so the negative `bulk_quantity = -9` data bug on VFG 7238 SHELL RIMULA falls back gracefully instead of returning a negative line value.
    - `computeVanStockItemValue(item)` — now `unitPrice × effectiveQty`. Same signature as before; existing 4 callers in `vanStockQueriesService.ts` keep working.
  - SELECT update (`services/vanStockQueriesService.ts`): added `avg_cost_per_liter, last_purchase_cost_per_liter` to all four `part:parts(...)` embed lists so the helper has the data it needs. Used `replace_all` on the single SELECT shape that appears 4× — typecheck confirmed all sites updated.
  - Modal change (`VanStockDetailModal.tsx`): swapped the Unit Price cell from `item.part?.cost_price` to `getVanStockItemUnitPrice(item)`. Now liquid rows display the per-liter rate (e.g. `RM 11.00/L` for GRANTT ATF) instead of the misleading per-container price. The Subtotal column was already using `computeVanStockItemValue` so it picked up the new math automatically.
  - DB audit view (`supabase/migrations/20260504_parts_liquid_price_drift_view.sql`): new read-only view `parts_liquid_price_drift` lists liquid SKUs where `cost_price` and `last_purchase_cost_per_liter` disagree by >5% — the case where the catalog has stored two different rates for the same item. First iteration of the formula was wrong (assumed cost_price was always per-container, divided by container_size, false-positived 3 of 4 liquid SKUs). Corrected formula compares the two columns directly — both should reflect the same per-unit rate regardless of unit choice. Applied to live DB; view reports exactly 1 drifting row (S-01044 GRANTT ATF, 1809.09% disagreement). Migration uses DROP+CREATE because PG can't rename view columns via CREATE OR REPLACE; safe since the view is read-only.
  - Live-DB verification (per-van after fix): BNX8936 6807→5809, BRK 3280 8235→5845, FA 8326 5848→4851, FA 9238 8441→6450, MDT 6631 7544→5554, VEW 9631 9817→5756, VEW8236 6416→5221 (matches Shin's 5218.50 within RM 2.85 rounding), VFA 6286 5671→4277, VFG 7238 10873→5043. Fleet-wide 69,652 → 48,808 (−20,844, including the −88 line value from VFG 7238 SHELL RIMULA's negative bulk-qty data bug). Shin should now reload the page and re-verify against Excel; expected delta is small (catalog rounding only).
  - Scope notes: did NOT update `parts.cost_price` for GRANTT ATF or any other SKU — Shin needs to confirm which rate is canonical (RM 11/L? RM 210/drum? something else?) before that catalog repair ships. Did NOT touch `services/jobInvoiceService.ts` which still uses `cost_price` for invoicing — invoicing semantics may legitimately want per-container pricing for sale, separate concern. Did NOT fix the `bulk_quantity = -9` data bug on VFG 7238 SHELL RIMULA — separate audit needed (probably a return-to-store flow that ran below zero). Did NOT alter the `computeVanStockItemValue` signature — same input shape, same return type, just smarter internal logic.
  - Verification: `npm run typecheck` exit 0. Migration applied to live DB; post-apply RAISE NOTICE confirmed exactly 1 drifting row. Helper signature uses `Pick<Part, ...>` so it accepts both the lean SELECT shape (in queries service) and the full `Part` type (in the modal) without casts.
  - Out of scope this commit (follow-ups for Shin/Phoenix): (a) confirm canonical rate for S-01044 GRANTT ATF and any future drift rows, then UPDATE the wrong column in `parts`; (b) investigate VFG 7238 SHELL RIMULA `bulk_quantity = -9` to find the broken transfer that produced it; (c) optionally surface the audit view in an admin dashboard tile so drift gets flagged proactively rather than only when someone reads the migration log.

[2026-05-05 04:41] [Opus] fix: Van Stock — total_value miscalc on liquid SKUs + add Unit Price/Subtotal columns so Admin 2 can verify against Excel — services/liquidInventoryService.ts, services/vanStockQueriesService.ts, pages/VanStockPage/components/modals/VanStockDetailModal.tsx, WORK_LOG.md, docs/CHANGELOG.md
  - Client report / context: Shin reported (5/4 5:04am MYT) that uploaded van stock amounts don't tally with the source Excel and asked us to surface item prices on the van stock screen so Admin 2 (`ADMIN_STORE` role) can reconcile per-row. There's no van-stock import flow in FT — items are entered via `AddItemModal` or `transferToVan`/`returnToStore`, so the discrepancy is between FT's computed totals and the manually-tracked Excel master, not between two upload paths.
  - Root cause (`services/vanStockQueriesService.ts` lines 33-36, 79-82, 90-93, 140-143): all four `total_value` reducers used `cost_price * item.quantity`. For non-liquid SKUs that's correct, but for liquid SKUs (`is_liquid=true`) the actual stock lives in `container_quantity * container_size + bulk_quantity` — `quantity` itself is meaningless on those rows, often 0. So a van carrying 5 sealed 10L drums of oil at RM50/L was computed as `50 × 0 = RM0` instead of `RM2,500`. The qty cell in `VanStockDetailModal.tsx:262` already does the dual-unit math correctly; the value reducers had drifted from it. The page-wide "Total Value" stat in `useVanStockData.ts:44` sums these per-van totals so it was wrong too. Independently, the items table had no price columns at all (Part / Qty / Min / Max / Status / Actions only) so Admin 2 had no way to reconcile.
  - Fix — single helper, shared math: added `computeVanStockItemValue(item)` to `services/liquidInventoryService.ts` next to `formatStockDisplay`/`getTotalBaseUnits` (same domain — that file already owns dual-unit logic). Helper branches on `item.part?.is_liquid`: liquid → `cost_price × ((container_qty × container_size) + bulk_qty)`; non-liquid → `cost_price × quantity`. Replaced all 4 reducer call sites in `vanStockQueriesService.ts` with `sum + computeVanStockItemValue(item)`. Then added two columns to the items table in `VanStockDetailModal.tsx`: **Unit Price** (right-aligned, `RM X.XX`, with `/L` suffix when `is_liquid`) and **Subtotal** (right-aligned, bold, computed via the same helper). Switched the table wrapper from `overflow-hidden` to `overflow-x-auto` since 8 columns exceeds the 768px modal width on small screens. Used `tabular-nums` so figures align across rows for visual scanning against an Excel column.
  - Approach rationale: chose the shared-helper option over an inline branch in each reducer (safer — one source of truth means future drift is impossible, mirrors the existing helper pattern in `liquidInventoryService.ts`). Used `cost_price` for the displayed unit price because the existing `total_value` figure is also cost-based — keeping the Subtotal × Qty math on screen consistent with the Total Value summary. `sell_price` could be added later if Admin 2 explicitly needs to reconcile against a sell-side Excel; deferred until asked.
  - Scope notes: did not touch `AddItemModal` (the issue is a calculation bug, not a data-entry bug — qty values themselves are correct on the rows), did not change `useVanStockData.ts` (it sums the now-correct per-van totals), did not touch the cards/grid (they consume `total_value` and benefit transparently). Did not add a migration — pure UI/calc fix, no schema or data changes. Did not add a unit price for `sell_price` (kept scope to what Admin 2 explicitly asked for; can be added if Shin clarifies).
  - Verification: `npm run typecheck` exit 0. Helper has zero side effects, signature uses `Pick<Part, ...>` so it accepts both `VanStockItem` and the service's `VanStockItemRow` without casts. Manual reasoning: a van with 5×10L sealed drums + 0 bulk + cost_price=RM50/L now reports RM2,500 (was RM0). A non-liquid SKU with qty=3 + cost_price=RM12 still reports RM36. Mixed van: sums correctly. Recommend Shin reload the Van Stock page and pick the van whose Excel total they trust most, then compare per-row Subtotal against Excel to spot any remaining disagreement (e.g. a wrong `cost_price` on a parts row would show up as the same per-unit error in both layers and is fixable from the parts catalog).

[2026-05-04 13:08] [Opus] feat: JobDetail — admin-editable Job Type, gated to pre-start statuses (mirrors description-edit pattern) — pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobEditActions.ts, pages/JobDetail/components/CustomerAssignmentCard.tsx, pages/JobDetail/JobDetailPage.tsx, WORK_LOG.md, docs/CHANGELOG.md
  - Client report / context: User asked to let "admin 1" (admin_service role) edit a job's `job_type` field, but only before the tech starts work — same gating pattern as the existing description edit. Three-layer change: state, handler, UI.
  - **State** (`useJobDetailState.ts`): added `editingJobType` (boolean) + `jobTypeInput` (`JobType | ''`). Initial value is `''` so the empty string serves as "unset" both for fresh edits and pre-Combobox state. Imported `JobType` from `../../../types`.
  - **Handlers** (`useJobEditActions.ts`): three handlers mirroring the description trio — `handleStartEditJobType` (populates input from current job_type, opens editor), `handleSaveJobType` (no-op early-return if value unchanged; otherwise calls `MockDb.updateJob({ job_type })` then `setJob({...updated})` per the realtime self-echo dedupe contract), `handleCancelJobTypeEdit` (resets state). Cast to `Partial<Job>` for the updateJob payload.
  - **UI** (`CustomerAssignmentCard.tsx`): new "Job Type" row inserted just above the Description row (so it groups with other editable job metadata). Visible to all roles (read-only for non-admins); Edit button shown only when `canEditJobType = !!onStartEditJobType && roleFlags.isAdminService && (statusFlags.isNew || statusFlags.isAssigned)`. The button has a tooltip explaining "Editable until the tech starts the job" so admins know why it disappears once status flips to In Progress. Editor uses the same `Combobox` component as CreateJob form, options sourced from `CREATABLE_JOB_TYPES.map(t => ({ id: t, label: JOB_TYPE_LABEL[t] }))` so the dropdown shows "General Service" / "Normal Service" while storing the canonical DB enum value. Save button disabled when input is empty OR equals the current value (prevents no-op writes). Cancel and Save mirror description-edit styling exactly.
  - **Gate semantics**: `roleFlags.isAdminService` already includes BOTH `admin` AND `admin_service` roles per `getRoleFlags` (`pages/JobDetail/utils.ts:132`) — so initially I had `(roleFlags.isAdmin || roleFlags.isAdminService)` and tightened to just `isAdminService` to match the description-edit pattern exactly (no behavior change; just removes redundancy). `admin_store` (parts/inventory admin) is NOT included by isAdminService, so they cannot edit job_type — matches the description-edit semantic. The status gate is `isNew OR isAssigned` because once the tech starts (status='In Progress'), changing job_type would invalidate hourmeter/checklist/parts validation already in flight.
  - **Wiring** (`JobDetailPage.tsx`): added 5 props passed through from state/actions to CustomerAssignmentCard, in the same chunk as the description-edit props above for consistency. New props are all optional in CustomerAssignmentCard's interface so existing test/storybook usage (if any) wouldn't break.
  - **Why no DB-side enforcement**: the existing `jobs` BEFORE-UPDATE triggers (audit log, parts confirmation check, before-photo enforcement) cover everything else. job_type changes during the New/Assigned window don't touch any of the post-start invariants (hourmeter, checklist, parts). The CHECK constraint on `jobs.job_type` already enforces valid enum values. App-layer gating is sufficient; adding a trigger-side block would be over-engineering for a pre-start window.
  - **Why no DB schema change**: pure UI/edit-flow feature. `jobs.job_type` already exists with the correct CHECK constraint. The Combobox sources values from `CREATABLE_JOB_TYPES` which are all valid CHECK members.
  - **Verification**: typecheck exit 0; vitest 48/48 still pass; build 531.52kb / 800kb (+0.49kb for the Combobox row + handlers). No new dependencies. No KPI math impact (pre-start jobs have 0 awards by construction; the synthesizer only fires events on completed/transferred jobs).
  - **Out of scope this commit**: editing job_type for jobs already in progress (would need to invalidate downstream validation state — explicit out-of-scope), bulk job_type edit (admin-side admin tool, not a JobDetail concern), audit-log entry for the job_type change (`log_job_changes` trigger already captures all column-level changes via the JSONB `old_value`/`new_value` mechanism).

[2026-05-04 13:02] [Opus] feat: KPI Engine — Q3 close-out, "Open Leave tab" CTA on dormancy banner — pages/People/components/KpiScoreTab.tsx, WORK_LOG.md, docs/CHANGELOG.md
  - Client report / context: Shin replied to the open Q3 ("attendance bonus is dormant — what to do?") with **option (a) — push team to file leaves properly**. Also confirmed the 9 techs without van assignments are intentional (in-office, share, or use helper vehicles). No further van work needed; just close out Q3.
  - **What ships**: tiny enhancement to the existing `LeaveDormancyBanner` (pages/People/components/KpiScoreTab.tsx). Banner copy updated to cite the 2026-05-04 decision explicitly. Added a CTA chip ("Open Leave tab" with ArrowRight icon) using `react-router-dom` `Link` to `/people?tab=leave` — the People page's existing `useSearchParams` tab routing handles the navigation. One-click path for admins to verify leave-filing status and nudge their team. Imported `Link` and `ArrowRight` to KpiScoreTab.
  - **Decision rationale**: option (a) is primarily a management/process action, not a code feature. The dormancy banner I shipped 2026-05-03 already surfaced the issue. The CTA closes the loop by giving the admin a single-click path from "I see the problem" to "I can act on it" — no need to navigate via the page tab bar. Options (b) reduce-bonus-weight and (c) source-attendance-elsewhere were not picked, so no math change to the engine.
  - **What I deliberately did NOT do**: did not change the bonus weighting (35/20/0 stays); did not build a tech-facing reminder ("file your leaves — affects your KPI") since that's a follow-on if the management push doesn't work; did not auto-email reminders to techs who haven't filed leave in N months (over-engineering for a problem that may resolve with one team meeting); did not touch the leave-filing UI itself (existing MyLeaveRequests page + LeaveTab admin view already cover the flow).
  - **Verification**: typecheck exit 0; vitest 48/48 still pass; build 531.03kb / 800kb (-0.04kb — net no growth, the new icon import is offset by tree-shaking).
  - **Q3 status: CLOSED.** All 3 client questions from 2026-05-03 now have shipped answers: Q1 (points table) applied 11:50, Q2 (transfer scope clarification + banner) shipped 11:50, Q3 (dormancy CTA) shipped 13:02.

[2026-05-04 12:54] [Opus] data: Van Stock — import 3 new vans (HISHAM/ONG/SHEN) from Shin's xlsx checklists handed over 2026-05-04 — supabase/migrations/20260504_van_stock_3_vans_addition.sql, WORK_LOG.md, docs/CHANGELOG.md
  - Client report / context: User pointed me at /home/jay/Downloads/Ft/ where Shin had dropped 9 xlsx files plus a tech-roster screenshot ("ACWER KOTA KEMUNING [AIE]") with 3 yellow-highlighted vans (BRK 3280, VFA 6286, FA 8326) matching the 3 newest xlsx files (May 4 timestamps). The other 6 xlsx files were already imported via 20260424_van_stock_6_vans_initial.sql. Mission: add the missing 3 to bring active van fleet from 6 → 9.
  - **Tech-to-van resolution** (per Shin's roster screenshot): HISHAM = MUHAMMAD HISHAM BIN SAIFUL = tech13@example.com (plate BRK 3280); ONG = ONG HENG WENG = tech21@example.com (plate VFA 6286, NB the xlsx content has a typo and says "YONG" — file naming and plate confirm it's ONG per the roster); SHEN = BEH CHOON SHEN = tech8@example.com (plate FA 8326). Pre-flight verified: all 3 techs had 0 existing van_stocks, no UNIQUE-on-technician_id conflict.
  - **Item parsing** (Python openpyxl, /tmp/parse): HISHAM 93 rows / 92 unique codes / 1 duplicate row (BUSH 20X25 appears twice — merged by summing qty 6+7=13); ONG 57 rows / 57 unique codes / 0 dups; SHEN 55 rows / 55 unique codes / 0 dups. After dedup: 92 + 57 + 55 = 204 unique line items across the 3 vans.
  - **Code-to-part_id reconciliation** (Node pg, /tmp/match): 138/139 unique codes matched parts.part_code directly; 1 fuzzy resolved (the SAME variant the prior 6-van migration documented) — `"23303-64010 B"` (with a stray space) + desc "FUEL FILTER 1182 @ NISSIN" → `23303-64010B` (no space). Occurs in HISHAM's xlsx (qty 2) and SHEN's xlsx (qty 3). All 139 codes resolved, zero remaining unmatched. Migration generation script saves /tmp/van_resolved.json with the full code→part_id map for traceability.
  - **Migration applied** (`20260504_van_stock_3_vans_addition.sql`): single BEGIN/COMMIT with three `WITH new_van AS (INSERT INTO van_stocks ...) INSERT INTO van_stock_items (...) SELECT ... CROSS JOIN VALUES (...)` blocks — same pattern as the 20260424 migration. `van_stock_items.UNIQUE (van_stock_id, part_id)` was the reason for the dedup step (block by constraint otherwise). Items default `min_quantity=1, max_quantity=5, is_core_item=true, container_quantity=0, bulk_quantity=0` matching prior pattern. Each van's `notes` field cites the source xlsx + check date for audit. `created_by_id/name` set to admin1@example.com / Admin One as the import actor. Post-apply DO block asserts exactly 3 new van_stocks + 204 van_stock_items present, raises EXCEPTION otherwise.
  - **Live verification**: `BRK 3280` (HISHAM) max=92, items=92, qty=292; `FA 8326` (SHEN) max=55, items=55, qty=232; `VFA 6286` (ONG) max=57, items=57, qty=162. Total active van_stocks: 6 → **9**. No errors during apply.
  - **Schema unchanged**: pure data load. Van Stock service code (`services/vanFleetService.ts` etc.), components (`pages/MyVanStock/`, `pages/StoreAdminDashboard/`), RLS policies, triggers — all untouched. The 3 new vans show up automatically in any existing van-stock UI surface for the assigned technicians.
  - **Out of scope this commit**: roster has 6 more techs without vans assigned (BEH PHENG SIANG, BEH CHOON SHYAN, EU SENG CHEONG, LEE KAI LUN, LIM KHER YUAN, MUHAMMAD SYUKRI TAUFIQ BIN RAFIZI, MUHAMMAD FADHIL BIN ISMAIL, MUHAMMAD SYAHRIL BIN BAHARUDDIN, LIM KIM HO) plus 2 helpers (MUHAMMAD NASHARUDEN, MUHAMMAD ZULFIQRIE). They're either in office, share vans, or work off the helper-vehicle. Wait for Shin to send their van assignments before importing.

[2026-05-04 11:50] [Opus] feat: KPI Engine — client feedback round (new points table + UI rename + transferred-job banner) — types/kpi.types.ts, types/job-core.types.ts, pages/JobBoard/components/JobCard.tsx, pages/JobBoard/components/JobListRow.tsx, pages/JobDetail/components/JobHeader.tsx, pages/JobDetail/components/TransferredJobBanner.tsx, pages/JobDetail/components/index.ts, pages/JobDetail/JobDetailPage.tsx, pages/CreateJob/CreateJobPage.tsx, components/TechnicianJobsTab/components/JobCard.tsx, components/InvoicePDF.tsx, components/ServiceReportPDF.tsx, tests/unit/kpi/points.spec.ts, tests/unit/kpi/synthesizer.spec.ts, WORK_LOG.md, docs/CHANGELOG.md
  - Client report / context: Shin (client) replied via WhatsApp with answers to the 3 outstanding questions. Two were direct go-aheads (new points table + UI rename), one was a clarifying question on transfer scope. All three actioned this commit.
  - **Q1 — new points table** (client confirmed via WhatsApp 11:21pm 2026-05-03, "draft KPI idea, will propose to management"): Slot-In=30 (was 20), Repair=25 (was 20), Service=20 (was 15), Full Service=15 (was 15), Field Technical Services=15 (was 15), Checking=5 (was 10). Updated `JOB_TYPE_POINTS` in `types/kpi.types.ts`. Legacy types (Minor Service, Courier) unmentioned in client's table — set to sensible defaults matching their successor types (Minor Service=15 as it predates Field Technical Services; Courier=5 matching Checking as light task). Test fixtures in `points.spec.ts` and `synthesizer.spec.ts` updated to reflect: spec test 1 (Continue Tomorrow Repair) now asserts 25 not 20; Service single-owner test asserts 20 not 15; Checking single-owner asserts 5 not 10; Assistance Repair pro-rata test (A=6h, B=2h) recomputed: 25 pts split as 6/8*25=18.75→19 (largest remainder gets +1) and 2/8*25=6.25→6 (sum=25); Checking 3-way Assistance test (A=5h B=2h C=1h) now sums to 5 with C getting the leftover unit (largest remainder 0.625) → A=3 B=1 C=1.
  - **Q2 — Transfer scope clarification** (client asked: "Tech B can view everything, including parts used by Tech A, but cannot make changes to records created before the transfer?"): YES — confirmed and made explicit. Architecture analysis: parent's status flips to `Incomplete - Reassigned` which excludes it from every action gate (`isInProgress` / `isAssignedToCurrentUser` / `!isCompleted`); the receiving tech is assigned to the CLONE, not the parent, so they pass NEITHER condition for parent edits → buttons don't render. To make this VISIBLY explicit (the gating was implicit before), added `TransferredJobBanner` component (`pages/JobDetail/components/TransferredJobBanner.tsx`) that appears at the top of the parent JobDetail when `statusFlags.isIncompleteReassigned`. Banner uses react-query to fetch all clones for this parent (sorted desc by created_at) and displays them as clickable chips with "{job_number} · {assigned_technician_name} →". Copy: "This job has been transferred — view only. All work has moved to the new job. The records here (parts, notes, history) are preserved as a snapshot at the moment of transfer; they cannot be modified."
  - **UI rename — DB-safe approach** (client confirmed via WhatsApp 11:24pm 2026-05-03): "Service" → "General Service", "Full Service" → "Normal Service" without changing functionality. Chose display-mapping over DB-rename because (a) `jobs_job_type_check` constraint pins the strings, (b) 940 rows would need backfill, (c) ~30+ code paths reference these strings (RLS policies, ACWER classifier, hourmeter logic, KPI engine, `JobType` enum). Approach: added `JOB_TYPE_LABEL: Record<JobType, string>` constant and `jobTypeLabel(jobType: string | null | undefined): string` helper in `types/job-core.types.ts`. Helper accepts arbitrary string input (e.g. raw DB row typed as `string`) and falls back to the input if no mapping — never breaks rendering. Updated 6 UI surfaces to consume via helper: JobBoard `JobCard.tsx` (badge), JobBoard `JobListRow.tsx` (2 badges — list + grid view), JobDetail `JobHeader.tsx` (status chip), TechnicianJobsTab `JobCard.tsx` (mobile badge), CreateJob `CreateJobPage.tsx` (Combobox dropdown — `JOB_TYPE_LABEL[t]` for label while `id=t` keeps the DB enum value), `InvoicePDF.tsx` (job type field with fallback "General Service"), `ServiceReportPDF.tsx` (checkbox text "GENERAL SERVICE" — both React and HTML-string variants). Backend logic, comparisons, RLS policies, triggers — all untouched.
  - **Re-ran April recompute with new points** — top scorer MUHAMMAD SYAHRIL: 1610 → **1820** (Δ +210). Math: 51 Repairs × (25-20)=+255, 24 Checkings × (5-10)=-120, 13 Service × (20-15)=+65, 9 Full Service × 0=0, 1 Slot-In × (30-20)=+10. Net +210. ✓. Leaderboard ranking shifted: BEE PHENG SIANG climbed to #6 (from old tied #6/7/8 cluster) because their job mix favored the new points distribution. Total job points across all techs went from 11,510 → ~12,200 (rough estimate). Snapshot count dropped from 19 → 18 because one tech ('Tech Two') had assignments but ZERO completed jobs in April — new SQL filter only counts techs with `completion_time` in period (correct semantics).
  - **Verification**: typecheck exit 0; vitest 48/48 still pass (after fixture updates); build 531.07kb / 800kb (+0.4kb for the banner component). Live DB: 18 April snapshots present, math verified by hand.
  - **Open**: Q3 (leave dormancy) — banner shipped last session, awaiting client decision on (a) push process change, (b) accept flat bonus, (c) revisit weighting. No action needed this commit.
  - **Out of scope this commit**: backend logic remains case-sensitive on the original DB strings ('Service', 'Full Service'). If anyone runs `WHERE job_type = 'General Service'` it WILL FAIL — and that's intended; the rename is UI-only by design.

[2026-05-03 06:33] [Sonnet] fix: KPI leaderboard data quality & visibility — filter never-worked techs, delete stale snapshots, add dormancy banner — services/kpiService.ts, pages/People/components/KpiScoreTab.tsx
  - Client report / context: Previous session's Phase 3 audit + dispute-notes + chart + cron shipped, but post-launch observation found 3 issues: (1) 3 test accounts showing zero points on leaderboard, (2) 21 techs all Elite tier (100% attendance bonus), (3) only 4 leave records in entire FT history suggest nobody files through FT. Requested "look into what's really happening before I get customer questions." Investigated, diagnosed root causes, and shipped targeted fixes.
  - **Issue 1 diagnosis: loadActiveTechnicians returns all 'technician' role users** — includes test accounts never assigned work. Fix: added `assigned_technician_id IS NOT NULL` filter (subquery reads all jobs, builds Set of techs who've been assigned, filters the users list). Source-level fix means future recomputes also won't create stale snapshots. Test accounts automatically re-appear when first assigned a real job.

<!-- Entries before 2026-05-03 trimmed — see git history -->

  - Scope notes: did not touch `job_service_records` where the real `technician_signature_at` lives — on reassign, any prior service record is orphaned by design (the job resets back to ASSIGNED, so the old tech's partial service record is no longer associated with the active workflow). Did not change DB_SCHEMA.md since it correctly scopes the column to `job_service_records`. Did not touch `rejectJobAssignment` — it doesn't clear signature fields at all.
  - Verification: queried live DB via pooler to confirm which columns exist on `jobs`; reproduced the update payload in a rolled-back transaction and confirmed it now succeeds; `npm run typecheck` clean. Remaining manual verification: admin opens a job in JobDetail, clicks Reassign, picks a different tech, hits Reassign — should succeed with "Job reassigned to <name>" toast.

[2026-04-09 01:37] [Sonnet] perf: JobBoard accept/reject 1.5–3s faster — trigger-based admin notifications + in-place job patching: pages/JobBoard/JobBoard.tsx, pages/JobBoard/hooks/useJobAcceptance.ts, pages/JobBoard/hooks/useJobData.ts, services/jobAssignmentCrudService.ts, supabase/migrations/20260409_notify_admins_on_accept.sql
  - Problem: technician accept/reject on JobBoard took 1.5–3 seconds. Root cause: (1) client-side `acceptJobAssignment` sequentially fetched all admin/supervisor users, then looped to insert a notification per-admin with `await`, blocking the accept response; (2) after accept, the client did a full `fetchJobs()` refetch, reloading every job with every embed (customer, media, schedule, etc.), another 1–2 second penalty.
  - Solution split DB/client: (1) DB: new migration `20260409_notify_admins_on_accept.sql` adds Postgres trigger `trg_notify_admins_on_job_accept` that fires when `technician_accepted_at` transitions NULL → NOT NULL, inserting all admin/supervisor notifications in one atomic `INSERT ... SELECT` inside the same transaction, zero client round-trips. (2) Client: new `patchJob` callback in `useJobData` patches a single job row in place instead of refetching; `useJobAcceptance` now calls `onJobPatched(updated)` with the fresh row returned by the service, eliminating the refetch.
  - Outcome: accept toast fires in <250ms. Job row updates in-place in the visible JobBoard list. Admin notifications delivered by DB trigger within 100ms.
  - Files changed: `pages/JobBoard/JobBoard.tsx` (+2 lines destructuring `patchJob`, passing to `useJobAcceptance`); `pages/JobBoard/hooks/useJobAcceptance.ts` (signature changed `onJobUpdated` → `onJobPatched`, accept/reject handlers call `onJobPatched(updated)` once, dropped `onJobUpdated()` call); `pages/JobBoard/hooks/useJobData.ts` (added `patchJob` callback, preserves `JobWithHelperFlag` augmentation on patch); `services/jobAssignmentCrudService.ts` (removed 11 lines of notification loop from `acceptJobAssignment`, function now just updates job row and returns it, dropped `getAdminsAndSupervisors` import); `supabase/migrations/20260409_notify_admins_on_accept.sql` (new, 82 lines, Postgres trigger function + trigger + post-apply sanity check).
  - Verification: `npm run typecheck` clean. Migration safety check: trigger is AFTER UPDATE on `technician_accepted_at` and checks `OLD.technician_accepted_at IS NULL`, so reassignments don't retrigger. Notifications are RLS-checked on read per the `notifications` table policy. In-place patching preserves existing row augmentations (e.g., `JobWithHelperFlag`). Considered alternatives: (a) `savingRef` flag per handler — fragile, would duplicate code; (b) debounce refetch — only narrows race; (c) used the architectural fact that `.select().single()` from the mutation is already canonical, so patch in place like JobDetail hooks do.


Format: `[YYYY-MM-DD HH:MM] [Agent] Summary`

<!-- Entries before 2026-03-06 trimmed — see git history -->
[2026-04-09 01:21] [Opus] fix: technician accept still failing with "could not embed because more than one relationship was found for jobs and job_media" — services/jobChecklistService.ts, services/jobMediaService.ts, services/jobAssignmentCrudService.ts, services/serviceScheduleService.ts, services/jobStatusService.ts, services/jobService.ts, services/jobInvoiceService.ts, services/supabaseClient.ts, services/customerService.ts
  - Client report: technicians still cannot accept assigned jobs; toast reads "Failed to accept job — could not embed because more than one relationship was found for jobs and job_media". Same symptom 132f1fa was supposed to fix on 2026-04-07.
  - Root cause: 132f1fa pinned every job_media embed with `media:job_media!job_id(*)`, trying to disambiguate the two FKs between jobs and job_media (`job_media.job_id → jobs.job_id` and the new `jobs.technician_rejection_photo_id → job_media.media_id` added the same day). The `!job_id` hint is a column-name hint, and PostgREST/Supabase's schema cache here does not resolve it reliably — the disambiguation is silently ignored and the ambiguity error returns. Option A from the diagnosis: switch from column-name hint to foreign-key constraint-name hint, which is PostgREST's canonical and always-reliable disambiguation mechanism.
  - Verification step before editing: connected to the live Supabase DB via the pooler (`pg` node client with SSL) and queried `pg_constraint` for the two FKs. Confirmed constraint names are `job_media_job_id_fkey` (the one we want) and `jobs_technician_rejection_photo_id_fkey` (the ambiguity source). Did not guess — used the actual constraint name from the DB.
  - Fix: replaced every `media:job_media!job_id(*)` with `media:job_media!job_media_job_id_fkey(*)` across all 9 service files (29 occurrences total). Grep confirms zero `job_media!job_id` stragglers outside the new form. No schema change, no RLS change, no trigger change, no frontend change — the FK constraint stays in place so referential integrity for `technician_rejection_photo_id` is preserved.
  - Scope notes: deliberately did not pursue Option B (drop the second FK) because that would lose DB-level referential integrity on rejection photos for the sake of shorter embed strings. Did not pursue Option C (two-step fetch in accept handler) because the ambiguity affects ~29 embed sites, not just accept — any job fetch with media was failing. Did not touch `pages/` because all accept paths already funnel through the service layer. Did not touch the FK constraint added by 20260407_fix_tech_rejection.sql. Did not change `forklifts!forklift_id` hints (those are unambiguous, only one FK).
  - Why this is durable vs a patch: (a) matches PostgREST's documented `embed!fk_constraint_name` contract; (b) stable against future FKs added between jobs and job_media (a third FK would not re-introduce ambiguity); (c) preserves RI; (d) local to query layer, no migrations.
  - Verification: `npm run typecheck` passes clean. Live DB query confirmed constraint name `job_media_job_id_fkey` exists. Remaining manual verification: technician clicks Accept on an assigned job in the live app — should succeed with "Job accepted" toast and no PostgREST embed error.

[2026-04-09 01:11] [Sonnet] chore: auto-commit session changes — CLAUDE.md, .claude/agents/ft-expert.md, scripts/smart-replace-rentals.py, .gitignore, WORK_LOG.md, docs/CHANGELOG.md, pages/JobDetail/hooks/useJobData.ts, pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobRealtime.ts, types/job-core.types.ts
  - Session 2026-04-09 05:10: diagnostic questions on client job acceptance issue. No code changes in this session.
  - Committing: two completed work items from 2026-04-08 (tooling stack + realtime self-echo fix) that were documented but not yet committed.
  - Changes: FT tooling stack (CLAUDE.md project reference, ft-expert agent, memory architecture, skills infrastructure), plus realtime self-echo dedupe fix for "could not save / signal aborted" on save mutations.
  - Verification: npm run typecheck clean. All staged files accounted for in prior session entries.

[2026-04-08 23:59] [Opus] chore: FT Tooling Stack — CLAUDE.md, Project-Scoped Agent, Skills, Memory Architecture — CLAUDE.md, .claude/agents/ft-expert.md, scripts/smart-replace-rentals.py, .gitignore
  - Context: FT sessions were starting from scratch every time — re-asking "what's the CHANGELOG format", "what does pre-commit require", "how does realtime interact with mutations", "where does each service live". Knowledge about conventions, landmines, and historical decisions was scattered and had to be re-derived on every session.
  - Solution: encode the entirety of FT's operational knowledge into three new files:
    - `CLAUDE.md` (project root): static reference for tech stack, scripts, WORK_LOG/CHANGELOG formats, pre-commit behavior, architecture landmarks, known landmines, the realtime self-echo gotcha with its `lastSeenUpdatedAtRef` fix, and the services-layer split. Auto-loaded by Claude Code whenever cwd is inside `/home/jay/FT`.
    - `.claude/agents/ft-expert.md`: project-scoped subagent for deep diagnosis, code review, architecture questions. Reads `CLAUDE.md` + `/home/jay/clawd/memory/projects/fieldpro.md` on every invocation, appends session observations to daily logs in the clawd memory tree for Phoenix Dream consolidation. Default mode is research-and-report; does not write code unless told.
    - `scripts/smart-replace-rentals.py`: utility to find and replace rental company names across jobs, service records, and customer records (added for reference; not invoked this session).
  - Supporting changes: `.gitignore` updated to exclude `.claude/memory/` (gitignored personal scratch notes). Two user-level skills previously added in `~/.claude/skills/`: `/ft-bugfix` (explore → diagnose → present options → stop for user approval → implement → typecheck → invoke `/ft-doc`) and `/ft-doc` (generates WORK_LOG.md + docs/CHANGELOG.md entries in FT's exact narrative format).
  - Supporting infrastructure: PostToolUse hook in `~/.claude/settings.json` auto-runs `npm run typecheck` after any Edit/Write inside FT. Memory architecture finalized: canonical living memory at `/home/jay/clawd/memory/projects/fieldpro.md` (tracked, consolidated by Phoenix Dream); gitignored scratch at `/home/jay/FT/.claude/memory/` (mid-session personal notes). The `/dream` command consolidates daily logs and promotes durable signals into `fieldpro.md`, pruning stale entries and handling dedup.
  - Motivation: the new stack encodes conventions once, so future sessions load them automatically without asking. The `/ft-bugfix` skill also enforces the user's documented "assess before execute" preference by making the approval gate a hard-coded phase. The `ft-expert` subagent lets diagnosis and review queries stay in-project without context bleed.
  - Files touched: `CLAUDE.md` (new, 131 lines), `.claude/agents/ft-expert.md` (new, 124 lines), `scripts/smart-replace-rentals.py` (new, 415 lines), `.gitignore` (3-line addition for `.claude/memory/`). No application code touched — pure tooling and infrastructure.
  - Verification: `npm run typecheck` passes clean. All files staged. docs/CHANGELOG.md updated with top-level entry documenting the tooling stack change.

[2026-04-08 14:11] [Opus] fix: technician "could not save / signal is aborted without reason" on save job done + recommendation — pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobData.ts, pages/JobDetail/hooks/useJobRealtime.ts, types/job-core.types.ts
  - Client report: technician trying to save Job Carried Out + Recommendation got a "Could not save" toast with "AboutError: signal is aborted without reason". Field actually persisted on next refresh — the abort fired after the DB write succeeded
  - Root cause: race between the save handler and the Supabase realtime self-echo. `handleSaveJobCarriedOut` (`pages/JobDetail/hooks/useJobActions.ts:744`) does the right thing — `await MockDb.updateJob(...)` and `setJob({...updated})`. But the postgres_changes UPDATE event for that same write is broadcast back milliseconds later. The realtime listener at `pages/JobDetail/hooks/useJobRealtime.ts:84` calls `onJobUpdated()` → `loadJob()` (`pages/JobDetail/hooks/useJobData.ts:35`), which fans out a fresh `getJobById` while the original save's PostgREST response is still in flight. The new request tears down the in-flight one, its AbortSignal fires, the save's `await` rejects with "signal is aborted without reason", and the catch-block toast displays the misleading "Could not save" — even though the DB row was already updated
  - Considered three fixes: (1) `savingRef` flag to suppress realtime during a save — fragile, has to be added to every mutation handler; (2) debounce realtime reload — only makes the race less likely; (3) treat the realtime echo as redundant work, since `setJob({...updated})` already applied the freshest possible row from the mutation's `select(...).single()` — chose (3) as the actual fix vs band-aids
  - Fix: `pages/JobDetail/hooks/useJobDetailState.ts` adds `lastSeenUpdatedAtRef = useRef<string|null>(null)` and bumps it inside `setJob()` whenever a job row with `updated_at` is applied locally. The ref is exposed on the state object. `pages/JobDetail/hooks/useJobData.ts` passes the ref to `useJobRealtime`. `pages/JobDetail/hooks/useJobRealtime.ts` short-circuits the postgres_changes UPDATE handler when `payload.new.updated_at === lastSeenUpdatedAtRef.current` — that's the self-echo of a write the local UI just made, so no toast and no `loadJob()`. Remote updates from other devices/sessions still flow through normally because their `updated_at` won't match
  - `types/job-core.types.ts` adds `updated_at?: string` to the `Job` interface. The DB column already exists and is in the realtime payload — only the TS type was missing it
  - Bonus wins: every other mutation handler in `useJobActions.ts` that follows the `setJob({...updated})` pattern (description, extra charges, parts, etc.) now also gets self-echo dedupe for free. Cuts a redundant `getJobById` round-trip off every save. Toasts like "Status changed to X" no longer double-fire on your own status changes
  - Scope notes: did not touch `useJobActions.ts` — the mutation handlers were already correct. Did not change the realtime listener for `job_requests` (that subscription has no race). Did not introduce per-handler flags. The fix is a single piece of state that covers every mutation
  - Verification: `npx tsc --noEmit` passes clean. Manual test path: technician opens a job, edits Job Carried Out + Recommendation, taps Save → success toast, no abort error, field persists. Cross-device test: second browser on same job changing status still produces the "Status changed" toast on the first browser
