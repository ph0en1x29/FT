[2026-04-20 23:21] [Sonnet] chore: cleanup stray debug script: .tmp_check.js
  - .tmp_check.js: removed stray debug script from FTS troubleshooting session

[2026-04-20 23:16] [Sonnet] fix: FTS job cannot complete — services/jobStatusService.ts, .tmp_check.js, WORK_LOG.md, docs/CHANGELOG.md
  - services/jobStatusService.ts: removed hourmeter + forklift gates from updateJobStatus that were blocking FTS job completion
  - .tmp_check.js: temporary file created during debugging session
  - WORK_LOG.md: updated with auto-commit entry
  - docs/CHANGELOG.md: updated with FTS completion fix documentation

[2026-04-21 02:30] [Opus] fix: FTS job cannot complete — service-layer gates block status update for jobs without forklift/hourmeter — jobStatusService.ts
  - Client report: FTS job still fails to update status (Complete). Earlier session already exempted FTS from hourmeter + checklist in the Start flow, the DB completion trigger, and every UI consumer (mobile workflow card, desktop JobDetailPage, sticky JobHeader button). Tech has sigs, after-photo, parts declared — Complete still errors.
  - Root cause (verified against live DB via pooler): FTS job JOB-260420-035 is stuck `status='In Progress'`, `forklift_id=null`, `hourmeter_reading=0`. The blocker is `services/jobStatusService.ts:37-40` — `updateJobStatus` has an FTS-blind service-layer gate: `if (!currentJob?.hourmeter_reading) throw 'Cannot complete job: Hourmeter reading is required'`. For FTS, `startJobWithCondition` writes `hourmeter_reading = forklift?.hourmeter || 0`, so FTS jobs without a forklift land at `0`. `!0 === true` → throws. The error bubbles through `handleStatusChange`'s catch → toast reads "Failed to update status" which is what the client experienced. Sibling bug: line 32-34 blocks `IN_PROGRESS` transitions when `forklift_id` is null (FTS is legitimately forklift-less). Live `validate_job_completion_requirements` trigger never checks hourmeter at all — the JS gate is stricter than the DB contract, redundant, and drifted.
  - Fix: removed both FTS-blind gates from `updateJobStatus`. Kept the technician-assigned check on IN_PROGRESS (universal) and the signature check on AWAITING_FINALIZATION (mirrors DB trigger, friendlier early-reject). Added a comment explaining why hourmeter + forklift gates live at the UI + DB layers only, not in the service.
  - Scope notes: did not touch `handleStatusChange` in useJobActions.ts — its FTS + helper + isFieldTech branching is already correct and the authoritative UI-layer gate. Did not touch `startJobWithCondition` — its hourmeter check is conditional on forklift presence and isn't hit for FTS. Did not touch the DB trigger — it already exempts FTS correctly. Did not touch BulkSignOffModal — it now inherits the fix since it calls `updateJobStatus` directly.
  - Verification: `npm run typecheck` clean. Queried live DB to confirm JOB-260420-035 state (FTS, no forklift, hourmeter=0, both sigs, 2 after-photos, In Progress). Remaining manual verification: that job's assigned technician taps Complete — should succeed with "Status updated to Awaiting Finalization" toast; DB trigger confirms sigs + parts + job_carried_out already present.


  - Client issue: desktop in-progress page still gated Complete button on raw `statusFlags.hasHourmeter` flag, even though completion logic + mobile UI already exempt FTS from hourmeter.
  - Root cause: three UI consumers without FTS exemption — completionBlocked (JobDetailPage.tsx:111), sticky Complete button (JobHeader.tsx:174-181), Condition Checklist card (JobDetailPage.tsx:224) — all keyed off raw flag that is false for FTS because no real reading is collected.
  - Fix: (1) JobDetailPage.tsx:109-116 — added `isFieldTechJob` + `hourmeterRequired = !isFieldTechJob && !hasHourmeter`, threaded into completionBlocked + "Hourmeter needed" chip. (2) JobDetailPage.tsx:226 — expanded Condition Checklist render guard to hide for both REPAIR and FIELD_TECHNICAL_SERVICES. (3) JobHeader.tsx:75-78 — computed `isFieldTech` + `hourmeterRequired` locally, swapped into sticky Complete button's disabled/className/tooltip.
  - Scope notes: did not change `statusFlags.hasHourmeter` in utils.ts — that raw flag is still used by hourmeter-edit UI. Kept per-consumer exemptions. Did not touch helpers (not reported — mobile card already handles FTS correctly).

  - Client follow-up: audit "anywhere else" the FTS hourmeter/checklist exemption is missing. Found three more unmirrored gates after the mobile fix.
  - Root cause: `useJobActions.handleStatusChange` exempted FTS from hourmeter + checklist, but three UI consumers were still keying off the raw `statusFlags.hasHourmeter` flag (which reads `!!job.hourmeter_reading` and is false for FTS because no real reading is collected at start). Additionally, the "Condition Checklist" collapsible card on `JobDetailPage.tsx:224` was hidden only for Repair, not FTS.
  - Fix: (1) `JobDetailPage.tsx:109-113` — added `isFieldTechJob = job?.job_type === FIELD_TECHNICAL_SERVICES` and `hourmeterRequired = !isFieldTechJob && !hasHourmeter`; threaded `hourmeterRequired` through `completionBlocked` and the "Hourmeter needed" amber chip at line 472. (2) `JobDetailPage.tsx:226` — expanded the Condition Checklist CollapsibleCard render guard to hide for both Repair AND FTS, matching `getMissingMandatoryItems` in utils.ts which already skips both types. (3) `pages/JobDetail/components/JobHeader.tsx` — computed `isFieldTech` + `hourmeterRequired` locally and swapped them into the sticky Complete button's `disabled` logic, className, and tooltip string; tooltip no longer says "Hourmeter reading required" for FTS, falling through to the next missing requirement.
  - Scope notes: did not change `utils.ts:hasHourmeter` — that raw flag is still consumed by the hourmeter-edit UI where the original signal is meaningful. Kept per-consumer exemption instead of flipping the flag globally. Did not touch helper exemption on the JobHeader button (not reported this session — helpers typically go through the mobile card which already handles their case). Did not change the BulkSignOffModal flow (no client-side hourmeter gate — relies on the DB trigger which never enforced hourmeter anyway).
  - Verification: `npm run typecheck` clean. Remaining manual verification: on desktop, open an FTS job → In Progress view should show no "Hourmeter needed" chip and no Condition Checklist card; Complete button in both the sticky header and the in-progress banner enables once after-photo + signatures + parts declaration are satisfied.

[2026-04-20 21:37] [Opus] fix: FTS Complete Job button perma-disabled on mobile — MobileTechnicianWorkflowCard.tsx
  - Client report (follow-up from the FTS start-flow fix earlier this session): since the Start Job modal no longer collects a real hourmeter for FTS, the mobile "Complete Job" button stays disabled with a "Hourmeter" chip in the blocker list, even though the `handleStatusChange` completion handler already exempts FTS from hourmeter validation.
  - Root cause: `MobileTechnicianWorkflowCard.tsx:33` builds its `blockers` array by checking `!statusFlags.hasHourmeter`, where `statusFlags.hasHourmeter` is computed in `pages/JobDetail/utils.ts:174` as `!!job.hourmeter_reading`. For FTS jobs, `hourmeter_reading` is passed through as the forklift's current reading at start (0 when the job has no forklift or a brand-new unit), so `hasHourmeter` is false → "Hourmeter" gets pushed into `blockers` → `canComplete` is false → Complete button is disabled. The desktop completion path goes through `handleStatusChange` directly and was already exempt, so this only surfaced on mobile.
  - Fix: added `const isFieldTech = job.job_type === JobType.FIELD_TECHNICAL_SERVICES;` and gated the "Hourmeter" blocker entry on `!isFieldTech && !statusFlags.hasHourmeter`. Matches the existing exemption pattern in `useJobActions.ts:341`. Imported `JobType` from `../../../types`.
  - Scope notes: did not change `statusFlags.hasHourmeter` in utils.ts — that flag is used in other contexts (e.g., rendering hourmeter edit UI) where the raw signal is still meaningful. Only the blocker-list consumer is mirrored. Did not touch helper exemption on the blocker list (not reported). Did not change partsDeclared / signature gates — those still apply to FTS as intended.
  - Verification: `npm run typecheck` clean. Manual test path: complete an FTS job end-to-end on mobile — Start Job → In Progress → add after photo + both signatures + tick "no parts used" → Complete Job button should be enabled (no "Hourmeter" chip visible).

[2026-04-20 21:27] [Opus] feat: hide hourmeter + checklist in Start Job modal for Field Technical Services — JobDetailModals.tsx, JobDetailPage.tsx, useJobActions.ts
  - Client report: for Field Technical Services jobs (on-site consultation, parts collection, charger/battery install), technicians should not be forced to fill hourmeter or checklist when starting the job.
  - Root cause: the completion flow already exempted FTS (`useJobActions.ts:328-342, 381-390` + DB trigger `validate_job_completion_requirements`), but the Start Job modal did not. `StartJobModal` received only `isRepairJob` (which hides the checklist); FTS still showed hourmeter AND checklist at Step 2, and `handleStartJobWithCondition` always validated hourmeter input.
  - Fix: (1) `JobDetailModals.tsx` — added `skipHourmeter?: boolean` prop that hides the hourmeter block and drops it from the Start button's disabled/hint logic; Step 2 header now reads "Checklist" / "Confirm Start" / "Hourmeter" / "Hourmeter & Checklist" depending on flags. (2) `JobDetailPage.tsx:338` — expanded `isRepairJob` to cover FTS (reuses existing checklist-hide) and passes `skipHourmeter={job.job_type === FIELD_TECHNICAL_SERVICES}`. (3) `useJobActions.ts:199-218` — `handleStartJobWithCondition` short-circuits the hourmeter parse/validate when FTS and passes the forklift's current reading through to `startJobWithCondition` so `jobs.hourmeter_reading` stays numeric for downstream queries.
  - Scope notes: did not rename `isRepairJob` (two call sites, one JSX spread — rename would bloat the diff). Did not touch the DB trigger — it already exempts FTS from checklist and never enforced hourmeter. Did not touch `handleOpenStartJobModal` hourmeter prefill — harmless since the input is hidden. Did not touch the Repair start flow.
  - Verification: `npm run typecheck` clean. Remaining manual verification: open an FTS job → "Start Job" → Step 2 should show only Before Photos gate (no hourmeter, no checklist) → Start button enabled after photos are added.

[2026-04-17 00:11] [Sonnet] auto-commit: session cleanup — useJobActions.ts, jobAssignmentBulkService.ts, jobRequestApprovalService.ts
  - Auto-committed session changes to code files updated during the previous session investigation.

<!-- Entries before 2026-04-19 trimmed — see git history -->

  - Scope notes: did not touch `job_service_records` where the real `technician_signature_at` lives — on reassign, any prior service record is orphaned by design (the job resets back to ASSIGNED, so the old tech's partial service record is no longer associated with the active workflow). Did not change DB_SCHEMA.md since it correctly scopes the column to `job_service_records`. Did not touch `rejectJobAssignment` — it doesn't clear signature fields at all.
  - Verification: queried live DB via pooler to confirm which columns exist on `jobs`; reproduced the update payload in a rolled-back transaction and confirmed it now succeeds; `npm run typecheck` clean. Remaining manual verification: admin opens a job in JobDetail, clicks Reassign, picks a different tech, hits Reassign — should succeed with "Job reassigned to <name>" toast.

[2026-04-09 01:37] [Sonnet] perf: JobBoard accept/reject 1.5–3s faster — trigger-based admin notifications + in-place job patching: pages/JobBoard/JobBoard.tsx, pages/JobBoard/hooks/useJobAcceptance.ts, pages/JobBoard/hooks/useJobData.ts, services/jobAssignmentCrudService.ts, supabase/migrations/20260409_notify_admins_on_accept.sql
  - Problem: technician accept/reject on JobBoard took 1.5–3 seconds. Root cause: (1) client-side `acceptJobAssignment` sequentially fetched all admin/supervisor users, then looped to insert a notification per-admin with `await`, blocking the accept response; (2) after accept, the client did a full `fetchJobs()` refetch, reloading every job with every embed (customer, media, schedule, etc.), another 1–2 second penalty.
  - Solution split DB/client: (1) DB: new migration `20260409_notify_admins_on_accept.sql` adds Postgres trigger `trg_notify_admins_on_job_accept` that fires when `technician_accepted_at` transitions NULL → NOT NULL, inserting all admin/supervisor notifications in one atomic `INSERT ... SELECT` inside the same transaction, zero client round-trips. (2) Client: new `patchJob` callback in `useJobData` patches a single job row in place instead of refetching; `useJobAcceptance` now calls `onJobPatched(updated)` with the fresh row returned by the service, eliminating the refetch.
  - Outcome: accept toast fires in <250ms. Job row updates in-place in the visible JobBoard list. Admin notifications delivered by DB trigger within 100ms.
  - Files changed: `pages/JobBoard/JobBoard.tsx` (+2 lines destructuring `patchJob`, passing to `useJobAcceptance`); `pages/JobBoard/hooks/useJobAcceptance.ts` (signature changed `onJobUpdated` → `onJobPatched`, accept/reject handlers call `onJobPatched(updated)` once, dropped `onJobUpdated()` call); `pages/JobBoard/hooks/useJobData.ts` (added `patchJob` callback, preserves `JobWithHelperFlag` augmentation on patch); `services/jobAssignmentCrudService.ts` (removed 11 lines of notification loop from `acceptJobAssignment`, function now just updates job row and returns it, dropped `getAdminsAndSupervisors` import); `supabase/migrations/20260409_notify_admins_on_accept.sql` (new, 82 lines, Postgres trigger function + trigger + post-apply sanity check).
  - Verification: `npm run typecheck` clean. Migration safety check: trigger is AFTER UPDATE on `technician_accepted_at` and checks `OLD.technician_accepted_at IS NULL`, so reassignments don't retrigger. Notifications are RLS-checked on read per the `notifications` table policy. In-place patching preserves existing row augmentations (e.g., `JobWithHelperFlag`). Considered alternatives: (a) `savingRef` flag per handler — fragile, would duplicate code; (b) debounce refetch — only narrows race; (c) used the architectural fact that `.select().single()` from the mutation is already canonical, so patch in place like JobDetail hooks do.


Format: `[YYYY-MM-DD HH:MM] [Agent] Summary`

<!-- Entries before 2026-03-06 trimmed — see git history -->
[2026-04-09 01:21] [Opus] fix: technician accept still failing with "could not embed because more than one relationship was found for jobs and job_media" — services/jobChecklistService.ts, services/jobMediaService.ts, services/jobAssignmentCrudService.ts, services/serviceScheduleService.ts, services/jobStatusService.ts, services/jobService.ts, services/jobInvoiceService.ts, services/supabaseClient.ts, services/customerService.ts
  - Client report: technicians still cannot accept assigned jobs; toast reads "Failed to accept job — could not embed because more than one relationship was found for jobs and job_media". Same symptom 132f1fa was supposed to fix on 2026-04-07.
  - Root cause: 132f1fa pinned every job_media embed with `media:job_media!job_id(*)`, trying to disambiguate the two FKs between jobs and job_media (`job_media.job_id → jobs.job_id` and the new `jobs.technician_rejection_photo_id → job_media.media_id` added the same day). The `!job_id` hint is a column-name hint, and PostgREST/Supabase's schema cache here does not resolve it reliably — the disambiguation is silently ignored and the ambiguity error returns. Option A from the diagnosis: switch from column-name hint to foreign-key constraint-name hint, which is PostgREST's canonical and always-reliable disambiguation mechanism.
  - Verification step before editing: connected to the live Supabase DB via the pooler (`pg` node client with SSL) and queried `pg_constraint` for the two FKs. Confirmed constraint names are `job_media_job_id_fkey` (the one we want) and `jobs_technician_rejection_photo_id_fkey` (the ambiguity source). Did not guess — used the actual constraint name from the DB.
  - Fix: replaced every `media:job_media!job_id(*)` with `media:job_media!job_media_job_id_fkey(*)` across all 9 service files (29 occurrences total). Grep confirms zero `job_media!job_id` stragglers outside the new form. No schema change, no RLS change, no trigger change, no frontend change — the FK constraint stays in place so referential integrity for `technician_rejection_photo_id` is preserved.
  - Scope notes: deliberately did not pursue Option B (drop the second FK) because that would lose DB-level referential integrity on rejection photos for the sake of shorter embed strings. Did not pursue Option C (two-step fetch in accept handler) because the ambiguity affects ~29 embed sites, not just accept — any job fetch with media was failing. Did not touch `pages/` because all accept paths already funnel through the service layer. Did not touch the FK constraint added by 20260407_fix_tech_rejection.sql. Did not change `forklifts!forklift_id` hints (those are unambiguous, only one FK).
  - Why this is durable vs a patch: (a) matches PostgREST's documented `embed!fk_constraint_name` contract; (b) stable against future FKs added between jobs and job_media (a third FK would not re-introduce ambiguity); (c) preserves RI; (d) local to query layer, no migrations.
  - Verification: `npm run typecheck` passes clean. Live DB query confirmed constraint name `job_media_job_id_fkey` exists. Remaining manual verification: technician clicks Accept on an assigned job in the live app — should succeed with "Job accepted" toast and no PostgREST embed error.

[2026-04-09 01:11] [Sonnet] chore: auto-commit session changes — CLAUDE.md, .claude/agents/ft-expert.md, scripts/smart-replace-rentals.py, .gitignore, WORK_LOG.md, docs/CHANGELOG.md, pages/JobDetail/hooks/useJobData.ts, pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobRealtime.ts, types/job-core.types.ts
  - Session 2026-04-09 05:10: diagnostic questions on client job acceptance issue. No code changes in this session.
  - Committing: two completed work items from 2026-04-08 (tooling stack + realtime self-echo fix) that were documented but not yet committed.
  - Changes: FT tooling stack (CLAUDE.md project reference, ft-expert agent, memory architecture, skills infrastructure), plus realtime self-echo dedupe fix for "could not save / signal aborted" on save mutations.
  - Verification: npm run typecheck clean. All staged files accounted for in prior session entries.

[2026-04-08 23:59] [Opus] chore: FT Tooling Stack — CLAUDE.md, Project-Scoped Agent, Skills, Memory Architecture — CLAUDE.md, .claude/agents/ft-expert.md, scripts/smart-replace-rentals.py, .gitignore
  - Context: FT sessions were starting from scratch every time — re-asking "what's the CHANGELOG format", "what does pre-commit require", "how does realtime interact with mutations", "where does each service live". Knowledge about conventions, landmines, and historical decisions was scattered and had to be re-derived on every session.
  - Solution: encode the entirety of FT's operational knowledge into three new files:
    - `CLAUDE.md` (project root): static reference for tech stack, scripts, WORK_LOG/CHANGELOG formats, pre-commit behavior, architecture landmarks, known landmines, the realtime self-echo gotcha with its `lastSeenUpdatedAtRef` fix, and the services-layer split. Auto-loaded by Claude Code whenever cwd is inside `/home/jay/FT`.
    - `.claude/agents/ft-expert.md`: project-scoped subagent for deep diagnosis, code review, architecture questions. Reads `CLAUDE.md` + `/home/jay/clawd/memory/projects/fieldpro.md` on every invocation, appends session observations to daily logs in the clawd memory tree for Phoenix Dream consolidation. Default mode is research-and-report; does not write code unless told.
    - `scripts/smart-replace-rentals.py`: utility to find and replace rental company names across jobs, service records, and customer records (added for reference; not invoked this session).
  - Supporting changes: `.gitignore` updated to exclude `.claude/memory/` (gitignored personal scratch notes). Two user-level skills previously added in `~/.claude/skills/`: `/ft-bugfix` (explore → diagnose → present options → stop for user approval → implement → typecheck → invoke `/ft-doc`) and `/ft-doc` (generates WORK_LOG.md + docs/CHANGELOG.md entries in FT's exact narrative format).
  - Supporting infrastructure: PostToolUse hook in `~/.claude/settings.json` auto-runs `npm run typecheck` after any Edit/Write inside FT. Memory architecture finalized: canonical living memory at `/home/jay/clawd/memory/projects/fieldpro.md` (tracked, consolidated by Phoenix Dream); gitignored scratch at `/home/jay/FT/.claude/memory/` (mid-session personal notes). The `/dream` command consolidates daily logs and promotes durable signals into `fieldpro.md`, pruning stale entries and handling dedup.
  - Motivation: the new stack encodes conventions once, so future sessions load them automatically without asking. The `/ft-bugfix` skill also enforces the user's documented "assess before execute" preference by making the approval gate a hard-coded phase. The `ft-expert` subagent lets diagnosis and review queries stay in-project without context bleed.
  - Files touched: `CLAUDE.md` (new, 131 lines), `.claude/agents/ft-expert.md` (new, 124 lines), `scripts/smart-replace-rentals.py` (new, 415 lines), `.gitignore` (3-line addition for `.claude/memory/`). No application code touched — pure tooling and infrastructure.
  - Verification: `npm run typecheck` passes clean. All files staged. docs/CHANGELOG.md updated with top-level entry documenting the tooling stack change.

[2026-04-08 14:11] [Opus] fix: technician "could not save / signal is aborted without reason" on save job done + recommendation — pages/JobDetail/hooks/useJobDetailState.ts, pages/JobDetail/hooks/useJobData.ts, pages/JobDetail/hooks/useJobRealtime.ts, types/job-core.types.ts
  - Client report: technician trying to save Job Carried Out + Recommendation got a "Could not save" toast with "AboutError: signal is aborted without reason". Field actually persisted on next refresh — the abort fired after the DB write succeeded
  - Root cause: race between the save handler and the Supabase realtime self-echo. `handleSaveJobCarriedOut` (`pages/JobDetail/hooks/useJobActions.ts:744`) does the right thing — `await MockDb.updateJob(...)` and `setJob({...updated})`. But the postgres_changes UPDATE event for that same write is broadcast back milliseconds later. The realtime listener at `pages/JobDetail/hooks/useJobRealtime.ts:84` calls `onJobUpdated()` → `loadJob()` (`pages/JobDetail/hooks/useJobData.ts:35`), which fans out a fresh `getJobById` while the original save's PostgREST response is still in flight. The new request tears down the in-flight one, its AbortSignal fires, the save's `await` rejects with "signal is aborted without reason", and the catch-block toast displays the misleading "Could not save" — even though the DB row was already updated
  - Considered three fixes: (1) `savingRef` flag to suppress realtime during a save — fragile, has to be added to every mutation handler; (2) debounce realtime reload — only makes the race less likely; (3) treat the realtime echo as redundant work, since `setJob({...updated})` already applied the freshest possible row from the mutation's `select(...).single()` — chose (3) as the actual fix vs band-aids
  - Fix: `pages/JobDetail/hooks/useJobDetailState.ts` adds `lastSeenUpdatedAtRef = useRef<string|null>(null)` and bumps it inside `setJob()` whenever a job row with `updated_at` is applied locally. The ref is exposed on the state object. `pages/JobDetail/hooks/useJobData.ts` passes the ref to `useJobRealtime`. `pages/JobDetail/hooks/useJobRealtime.ts` short-circuits the postgres_changes UPDATE handler when `payload.new.updated_at === lastSeenUpdatedAtRef.current` — that's the self-echo of a write the local UI just made, so no toast and no `loadJob()`. Remote updates from other devices/sessions still flow through normally because their `updated_at` won't match
  - `types/job-core.types.ts` adds `updated_at?: string` to the `Job` interface. The DB column already exists and is in the realtime payload — only the TS type was missing it
  - Bonus wins: every other mutation handler in `useJobActions.ts` that follows the `setJob({...updated})` pattern (description, extra charges, parts, etc.) now also gets self-echo dedupe for free. Cuts a redundant `getJobById` round-trip off every save. Toasts like "Status changed to X" no longer double-fire on your own status changes
  - Scope notes: did not touch `useJobActions.ts` — the mutation handlers were already correct. Did not change the realtime listener for `job_requests` (that subscription has no race). Did not introduce per-handler flags. The fix is a single piece of state that covers every mutation
  - Verification: `npx tsc --noEmit` passes clean. Manual test path: technician opens a job, edits Job Carried Out + Recommendation, taps Save → success toast, no abort error, field persists. Cross-device test: second browser on same job changing status still produces the "Status changed" toast on the first browser
