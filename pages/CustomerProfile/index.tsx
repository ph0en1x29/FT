// CustomerProfile folder index
// Re-exports the main component to maintain import compatibility
// Usage: import CustomerProfile from './pages/CustomerProfile' (same as before)

export { default } from './CustomerProfilePage';
export type { CustomerProfileProps } from './types';
